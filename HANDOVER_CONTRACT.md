# ARC-TXT 명세 재작성 인수인계 계약서

**수신**: 명세서를 재작성할 담당(Claude Chat)
**목적**: 구현 중 실제로 발생한 결함 54건을 조항으로 고정해 재발을 원천 차단한다.
**지위**: 이 문서는 명세서보다 **상위**다. 명세와 충돌하면 이 문서가 이긴다.
**작성 근거**: 6개 관점 적대적 감사(미래누수·생존자편향·수치통계·실데이터크래시·명세준수·캐시인덱스)에서
확인된 43건 + 실제 Colab 실행 로그에서 드러난 10건 + 프로세스 실패 1건. **전부 재현 완료된 것만 적었다.**

---

## 제0장 총칙

### 제0조 (해석 원칙)
1. 모든 조항은 **금지**·**의무**·**검증**의 3부로 구성한다. 검증 방법이 없는 조항은 조항이 아니다.
2. "하지 않는 것이 좋다"는 표현을 쓰지 않는다. **하지 않는다** 또는 **한다**로만 쓴다.
3. 어떤 조항도 "성능이 안 나오면 조정한다"는 예외를 갖지 않는다.

### 제0조의2 (작업 보고의 진실성)
- **금지**: 적용하지 않은 수정을 적용했다고 보고하는 것.
- **의무**: 코드를 수정한 뒤 **반드시 grep 또는 재실행으로 실제 반영을 확인**한 다음 보고한다.
- **근거**: 실제로 발생함. 일괄 수정 스크립트가 assertion 에서 중단됐는데 확인 없이 커밋해,
  커밋 메시지에 적힌 수정 2건이 파일에 존재하지 않았다.
- **검증**: 커밋 전 `grep -c "<수정의 고유 문자열>" <파일>` 이 1 이상.

---

## 제1장 파이프라인 순서 규약 ★ 최우선

> 이 장이 이 계약서의 핵심이다. 여기를 어기면 나머지를 다 지켜도 실행이 끝나지 않는다.

### 제1조 (유니버스 우선 원칙)
데이터 수집은 반드시 **3단계 깔때기**로 설계한다. 단계를 건너뛰거나 순서를 바꾸지 않는다.

```
[1단계] 유니버스 확정에 필요한 최소 데이터만 수집
        └ 대상: 종목 마스터(상장·폐지·상장일·시장·증권종류)
        └ 산출: 리밸일별 U-1000 멤버십
[2단계] 1단계에서 확정된 U-1000 합집합에 대해서만 본수집
        └ 대상: 일봉 / DART 재무 / 정기보고서 원문 / 애널리스트 리포트
[3단계] 2단계 대상에 대해서만 3차 필터·신호 산출
        └ 배제 플래그 · 하드팩트 · 텍스트 유사도 · 톤 분류
```

- **금지 ①**: 1단계 이전에 전 종목 일봉·재무·원문을 수집하는 것.
- **금지 ②**: U-1000 후보가 될 수 없는 종목의 데이터를 1건이라도 수집하는 것.
  후보 불가 = 스팩 · 우선주 · 리츠 · KONEX · 수익증권 · 투자회사 · 선박투자회사 ·
  신주인수권증서 · 백테 구간(시작−18개월 ~ 종료) 밖 종목.
- **금지 ③**: 3차 필터를 전 종목에 적용한 뒤 그 결과로 유니버스를 좁히는 것(순서 역전).
- **의무**: 각 단계 진입 시 **대상 종목 수와 직전 단계 대비 감소량·감소 사유를 표로 출력**한다.
- **근거**: 시총 하위 1000 이 목표인데 일봉을 5,398종목 전부 수집해 12분 + DART 를
  3,981사 전부 수집해 205,890콜을 요구했다. 실제로는 2,946종목 / 2,400사면 충분했다.
- **검증**: 계약검정 — 합성 마스터 5,000종목 투입 시 일봉 수집 대상이 60% 이하로 줄고,
  줄어든 종목 중 U-1000 에 편입될 수 있는 종목이 0건임을 확인.

### 제1조의2 (1단계의 최소성)
- **의무**: 1단계는 **월말 종가 + 상장주식수**만으로 시가총액을 만들 수 있어야 한다.
  KRX 인증이 실패해도 1단계는 완주해야 한다.
- **금지**: 1단계가 인증 필요 소스(pykrx 스냅샷 등)에 **의존**하는 것. 보강은 허용, 의존은 금지.
- **근거**: 시총 폴백 경로가 dtype 문제로 죽어 U-1000 이 0행이 되고 실행 전체가 중단됐다.

### 제1조의3 (18개월 여유 구간)
- **의무**: 구간 밖 종목을 뺄 때 경계는 **백테 시작 −18개월**로 잡는다.
- **근거**: 모멘텀 12-1 과 ADTV60 이 리밸일 이전 데이터를 쓴다. 좁게 자르면 생존자편향이 되살아난다.

---

## 제2장 외부 API 호출 규약

### 제2조 (응답 재사용 의무)
- **의무**: API 응답에 포함된 **모든 기간의 값을 보존**한다. 응답이 이미 담고 있는 기간을
  다시 호출하지 않는다.
- **적용례**: DART `fnlttSinglAcntAll` 은 당기·전기·전전기 3개 연도를 한 번에 준다.
  12년치는 4콜이면 전수 커버된다(3년 간격 앵커).
- **금지**: `thstrm_amount` 만 남기고 `frmtrm_amount` / `bfefrmtrm_amount` 를 버리는 것.
- **근거**: 이것 하나로 호출량이 3배였다. 205,890콜 중 약 2/3 가 이미 가진 데이터의 재구매였다.
- **검증**: 계약검정 — 1콜 응답을 파싱했을 때 반환 행의 `bsns_year` 가 2개 이상.

### 제2조의2 (PIT 안전장치, 제2조의 필수 부속)
- **의무**: 전기·전전기 값의 `knowledge_date` 는 **그 값을 실제로 알게 된 시점**
  (= 해당 응답을 받은 보고서의 접수일)로 한다.
- **금지**: 해당 사업연도의 원래 보고서 접수일을 쓰는 것. 그 시점엔 정정 이전 값이었을 수 있다.
- **의무**: 해당 연도 보고서를 직접 호출한 값이 나중에 확보되면 **그것으로 대체**한다
  (출처연도 − 사업연도 가 작은 쪽이 이긴다).
- **의무**: '그 연도 보고서에서 직접 받은 값'의 비중을 로그로 출력한다.

### 제3조 (호출 상한을 미리 정하지 않는다)
- **금지**: 일일 한도를 상수(19,000 / 20,000 등)로 가정하고 그것을 상한처럼 쓰는 것.
- **의무**: 실측 학습값이 없으면 **상한 없이** 진행하고, 서버가 한도초과를 반환한 지점을 학습한다.
- **의무**: 학습값에는 **하한**(힌트의 50%)과 **만료**(30일)를 둔다.
- **금지**: 파라미터 오류 응답(DART `021` = 조회 회사 수 초과)을 한도초과(`020`)와 같이 처리하는 것.
- **근거**: 힌트 20,000 을 상한으로 써서 "약 12일 걸립니다"라는 잘못된 안내를 했다.
  `021` 한 번이 예산 원장에 `learned_limit=1` 을 영구 기록해 이후 모든 날 하루 100콜로 고정시켰다.
- **검증**: 계약검정 — `021` 응답 후에도 예산이 소진 처리되지 않고 후속 수집이 계속됨.

### 제4조 (폴백 체인의 자기 차단)
- **의무**: 폴백 소스가 **성공 0건 · 연속 실패 N건**이면 그 실행에서 해당 소스를 끈다.
- **의무**: 소스별 **성공/빈손 건수를 표로 출력**한다.
- **금지**: 데이터가 존재할 수 없는 대상에 폴백을 시도하는 것
  (예: 한국 상장폐지 종목에 yfinance, 시장을 알면서 `.KS`/`.KQ` 둘 다 시도).
- **근거**: pykrx 가 0성공 / 1,322빈손인데 체인 맨 앞이라 종목마다 헛호출했다.
  폐지 코드에 yfinance 를 2회씩 때려 404 로그 수천 줄과 종목당 수 초를 태웠다.

### 제5조 (증분 수집의 정확성)
- **의무**: 캐시에 **빠진 구간만** 요청한다. 앞 구간이 비었다고 전체를 다시 받지 않는다.
- **의무**: 앞 구간(head)과 뒤 구간(tail) 결손을 **각각 독립적으로** 판정한다.
- **근거**: 캐시 최소일이 요청 시작일보다 늦다는 이유로 1,049종목의 10년치를 통째로 재수집했다.

### 제5조의2 (음성 캐시)
- **의무**: '정상적으로 데이터가 없는 조합'(상장 전/폐지 후 연도 등)을 원장에 기록해 재조회하지 않는다.
- **금지 ①**: 네트워크 오류·예산 소진·파싱 실패를 음성 캐시에 기록하는 것.
  **데이터 없음이 서버 응답으로 확인된 경우에만** 기록한다.
- **금지 ②**: 음성 캐시의 만료 기준을 **실행일이 아닌 값**(백테 종료일 등)으로 잡는 것.
- **근거**: 가격 음성캐시가 `attempted_at` 과 비교 기준을 **둘 다 BACKTEST_END** 로 써서
  경과일이 항상 0 → 한 번 실패한 종목이 영구 블랙리스트가 됐다. 첫 실행 중 몇 분간
  네트워크가 막힌 종목이 U-1000 에 영영 들어오지 못한다.
- **검증**: 계약검정 — 실패 기록 후 만료일이 지난 시점을 모사하면 재시도 대상에 다시 포함됨.

### 제6조 (실행 순서 의존성)
- **의무**: 인증이 필요한 자원은 **인증 완료 후에** 워밍업/조회한다. 워밍업 결과를 캐시했다면
  인증 성공 시 **무효화**한다.
- **근거**: KRX 로그인이 성공했는데도 시총 스냅샷 80개 시점을 전부 건너뛰었다.
  세션 워밍업이 로그인보다 먼저 돌아 '세션 없음'으로 굳었기 때문이다.

---

## 제3장 캐시·인덱스 규약 (절대 원칙)

### 제7조 (기존 캐시 불가침)
- **금지 ①**: 기존 인덱스·테이블을 **훼손**하는 것. 삭제 API 를 만들지 않는다.
- **금지 ②**: 공용 테이블을 **요청 구간으로 잘라서 되쓰는** 것.
  저장용 프레임과 소비용 프레임을 분리한다.
- **금지 ③**: 수집 실패로 빈 프레임이 나왔을 때 기존 캐시를 덮어쓰는 것.
  공용 테이블이 기존 행수의 50% 아래로 줄면 교체하지 않고 리비전 파일로만 남긴다.
- **의무**: 교체 전 반드시 백업. 백업 실패 시 덮어쓰지 않는다.
- **의무**: 백업은 **세대 상한**(테이블당 3개)을 둔다.
- **근거**: 일봉 테이블을 절단 저장해 다른 전략이 넣어둔 2010~2015 구간이 사라졌다.
  `_backup` 이 무한 증식하면 드라이브가 차고, 그 순간 신규 수집분이 통째로 소실된다.

### 제8조 (전략 간 공용 인덱스)
- **의무**: 공용 네임스페이스(`_shared`)는 **전략 간에 실제로 공유되어야 한다.**
  전략별 볼트 루트가 다르면, **다른 전략 볼트의 `_shared` 를 읽기 전용으로 함께 조회**한다.
- **금지**: 남의 볼트에 쓰는 것.
- **의무**: 재사용 시 **몇 행을 어디서 가져왔는지** 로그로 남긴다.
- **근거**: ARC 는 `/MyDrive/quant_cache`, 다른 전략은 `/MyDrive/tcd_cache` 에 각자 `_shared` 를
  갖고 있어 서로를 전혀 못 봤다. 이미 받아둔 일봉 709만 행을 눈앞에 두고 재수집했다.
  공용 인덱스를 만든 취지 자체가 무너져 있었다.

### 제9조 (드라이브 마운트 실패의 처리)
- **금지**: 마운트 실패를 **조용히 로컬로 폴백**하는 것.
- **의무**: 재시도(3회, 2회차부터 강제 재마운트) 후에도 실패하면 **무엇이 잘못되는지 명시하고 중단**한다.
  계속하려면 사용자가 명시적 플래그를 설정해야 한다.
- **근거**: 마운트가 조용히 실패해 드라이브 캐시가 전혀 안 보이는 상태로 전량 재수집이 시작됐고,
  새로 받은 데이터도 드라이브 밖에 쌓였다.

### 제10조 (읽기 실패 ≠ 파일 손상)
- **금지**: parquet 읽기 실패를 곧바로 손상으로 단정해 파일을 개명하는 것.
  드라이브 FUSE 의 `OSError(5)`, 스트리밍 마운트 미실체화, arrow 버전 문제도 같은 예외로 온다.
- **의무**: 짧은 백오프로 재시도 → 매직바이트(`PAR1`) 확인 → 진짜 손상일 때만 격리하되
  **`.parquet` 확장자를 유지**해 회수 가능하게 둔다.
- **근거**: 일시적 IO 오류로 멀쩡한 캐시를 `.corrupt.<ts>` 로 개명해 영구 고아로 만들었다.
  `get_table` 도 흡수 스캔도 그 파일을 찾지 못한다 — 삭제와 구분되지 않는다.

### 제11조 (인덱스 멱등성)
- **금지**: 인덱스 행의 식별자(uid)를 **행의 위치**로 생성하는 것.
- **의무**: 식별자는 **행의 내용만으로** 해시한다.
- **근거**: 레거시 행의 uid 에 위치 인덱스가 들어가 컴팩션마다 같은 행이 새 행으로 인식됐다
  (5행 → 10 → 15 → 20). 20만 행 인덱스면 실행마다 유령이 증식한다.

### 제12조 (기존 캐시 흡수 스캔)
- **금지 ①**: 볼트 자기 자신(및 다른 볼트)의 `blob`/`table`/`index` 디렉터리를 스캔 대상에 넣는 것.
- **금지 ②**: 파일마다 대기 목록을 선형 탐색하는 것(O(n²)).
- **의무**: 시간 예산·파일 상한·주기적 진행 로그를 둔다. 예산 초과 시 남은 경로를 보고하고 중단한다.
- **의무**: 흡수 실패가 **실행을 막지 않는다**(부가 기능이지 전제가 아니다).
- **근거**: 볼트 자기 blob 을 다시 등록하면서 O(n²) 로 돌아 실행이 사실상 멈췄다.

---

## 제4장 PIT 규약 (미래누수 금지)

### 제13조 (관측 시점 라벨링)
- **의무**: 모든 관측치의 날짜 라벨은 **실제 관측일**이다. 요청한 격자일이 아니다.
- **금지**: 리밸일 당일 종가로 계산한 값을 리밸일로 라벨링하는 것.
- **의무**: 소비 측 `merge_asof` 는 `allow_exact_matches=False` 로 당일을 명시적으로 배제한다.
- **근거**: PIT 시가총액이 리밸일 **당일 종가** 기준인데 라벨은 리밸일이었다.
  당일 종가로 유니버스를 정하고 당일 시가로 체결하면, 급락한 종목이 시총이 줄어
  하위 1000 안으로 들어와 급락 직전 시가에 매수 가능해진다.

### 제14조 (표준화 셀의 시간 폐쇄성)
- **금지**: 횡단면 표준화·윈저라이즈의 셀이 **미래 관측치를 포함**하는 것.
- **의무**: 셀은 **동시 관측 코호트**로 잡는다.
  문서 유사도는 `사업연도|보고서종류`, 재무지표는 `분기`, 최종 점수는 `분기|섹터`.
- **금지**: 달력연도를 셀로 쓰는 것(3월 접수분의 평균·표준편차가 11월 접수분으로 계산된다).
- **금지**: 윈저라이즈 경계·강제최하위 값을 전 기간에서 구하는 것.
- **검증**: 계약검정 — **미래 코호트의 값만 바꿨을 때 과거 코호트의 점수가 변하지 않을 것.**
  이 검정을 축마다 하나씩 둔다. (D1·D2 양쪽에서 실제로 위반이 나왔다.)

### 제14조의2 (조용한 폴백 금지)
- **금지**: 셀 키 구성 컬럼이 없을 때 조용히 다른 키로 폴백하는 것.
- **의무**: 필수 키가 없으면 **예외를 던진다.**
- **근거**: 셀을 코호트로 고쳤는데 그 컬럼이 프레임에 없어 가드가 항상 빗나가고
  달력연도로 폴백했다. 문자열 연결이 정상 동작해 그럴듯한 키가 만들어져 예외도 없었다.
  **수정했다고 믿었지만 무동작이었다.**

### 제15조 (이벤트 판정의 단방향성)
- **금지**: 문서/관측 시점 기준 **±N일 양방향**으로 이벤트를 매칭하는 것.
- **의무**: `0 ≤ (관측일 − 이벤트일) ≤ N` 만 인정한다.
- **근거**: 구조적 변화 플래그가 `abs(차이) ≤ 365` 라 **문서 접수 이후** 1년의 공시까지 매칭했다.
  "앞으로 12개월 안에 합병을 공시할 기업"이라는 미래 정보로 그 종목의 점수 구성이 바뀌었다.

### 제16조 (편입 자격의 시점 폐쇄성)
- **금지**: 편입 자격을 **미래 정보로 조건 지우는** 것.
  특히 전방수익률 결측 여부를 편입 필터에 쓰지 않는다.
- **의무**: 편입 자격은 **그 시점의 신호 유무**만으로 판정한다.
- **의무**: 전방수익률은 **유니버스 멤버십과 무관한 전 종목 체결가 격자**에서 계산한다.
- **근거**: `elig = 신호 있음 AND 전방수익률 있음` 이라, 다음 분기에 유니버스를 이탈하는 종목
  (= 크게 오른 종목, 붕괴 중인 종목)이 **오늘의 편입 후보에서 삭제**됐다. 방향성 있는 편향이다.

### 제17조 (정정공시)
- **금지**: 정정공시가 원본을 프레임에서 대체하는 것.
- **의무**: 원본이 있으면 정정본은 신호에서 제외(플래그만 기록). 정정본밖에 없으면 유지.
- **근거**: 정정공시를 내는 기업은 부실기업 쪽으로 치우쳐 있어 무작위가 아닌 표본 손실이 된다.

### 제18조 (결정성)
- **금지**: 파이썬 내장 `hash()` 를 신호 산출 경로에 쓰는 것.
  CPython 문자열 해시는 프로세스마다 랜덤화된다.
- **의무**: 결정적 해시(blake2b 등)를 쓴다.
- **검증**: 계약검정을 **별도 프로세스 2회 실행**으로 한다. 동일 프로세스 내 2회는 이 결함을 잡지 못한다.

---

## 제5장 생존자편향 규약

### 제19조 (상장폐지 손익의 완전 계상)
- **의무**: 폐지 판정은 **폐지목록 전수 기준**으로 한다. 패널에 행이 남아 있는 종목만 보지 않는다.
- **의무**: '보유 중 매도 불가(거래정지) → 폐지' 경로를 별도로 판정해 청산 결과를 계상한다.
- **의무**: 계상 건수를 **폐지목록 전수 기준 건수와 대조**하고 불일치 시 경고한다.
- **근거**: 거래정지 1개월이면 시총 격자에서 먼저 사라져 폐지 시점에 평가할 행이 없다.
  한국의 감사의견거절·자본잠식 폐지는 대부분 이 경로다 — **손실만 선택적으로 소실됐다.**

### 제20조 (폐지 사유의 구분)
- **금지**: 모든 폐지에 −100% 를 붙이는 것.
- **의무**: 청산가는 **폐지일 직전 종가**를 쓴다. 종가조차 없을 때만 −100%.
- **의무**: 폐지 사유(`Reason`)를 수집해 정상 폐지(합병·완전자회사화·자진상장폐지) 비중을 보고한다.
- **근거**: 실제 폐지목록의 16~34% 가 정상 사유다. 이들은 합병비율·공개매수가로 원금 수준이
  회수되는데 전부 전손으로 계상됐다.

### 제20조의2 (거래정지 후 재개의 구분)
- **금지**: '전방가격 없음 + 언젠가 폐지됨'만으로 폐지 청산 처리하는 것.
- **의무**: 폐지 전에 거래가 재개된 시점이 있으면 **거기서 청산**한다.
- **의무**: 진입 체결가가 없는 행은 애초에 보유가 성립하지 않으므로 손익을 계상하지 않는다.
- **근거**: 정지 후 재개된 종목을 폐지로 오인해 **3년 9개월 뒤 수익률이 1분기 자리**에 들어갔다
  (`fwd_ret_1q = +400%`, 같은 행 `fwd_ret_2q = +20%`).

### 제21조 (상장일의 진위)
- **금지**: 상장일이 없을 때 **첫 관측일로 채운 값을 진짜 상장일처럼** 쓰는 것.
- **의무**: 소스가 상장일을 제공하면 반드시 쓴다(폐지목록 CSV 에도 `ListingDate` 가 있다).
- **의무**: 추정한 상장일은 출처를 기록하고 **시즈닝·최소상장기간 필터의 근거로 쓰지 않는다.**
- **근거**: 폐지목록의 상장일을 버리고 첫 스냅샷 날짜(≈백테 시작월)로 채운 뒤 250거래일
  시즈닝을 걸어 **폐지 종목만** 초기 구간에서 사라졌다. 생존군과 폐지군에 다른 규칙이 적용됐다.

### 제22조 (정적 제외의 정밀성)
- **금지**: 종목명 **부분문자열**로 종목 유형을 판정하는 것.
- **의무**: 어미 기준 정규식 + 증권종류(`SecuGroup`) 필드를 함께 쓴다.
- **의무**: 시장 필터(KOSPI/KOSDAQ)를 명시적으로 적용한다. 시장 정보가 없으면 제외하지 않는다.
- **근거**: `리츠` 부분문자열이 메리츠금융지주·메리츠증권·메리츠화재·블리츠웨이를 제외했다.
  시장 필터가 없어 KONEX 109종목과 수익증권·선박투자회사가 U-1000 에 들어왔다
  (시총 극소라 하위 1000 컷 안쪽에 확정 편입되어 정규 소형주를 밀어낸다).

---

## 제6장 결측 규약

### 제23조 (결측을 0으로 채우지 않는다)
- **금지**: 신호·재무 항목의 결측을 0 으로 채우는 것.
- **의무**: 결측은 결측으로 두고 **가중치를 재배분**한다.
- **근거**: 순영업자산 계산에서 현금 결측을 0 으로 채워, 현금 라인만 못 읽은 법인이
  체계적으로 불리해졌다(방향 −1 지표). `fillna` 가 NaN 을 없애 커버리지 표에는 100% 로 보고됐다.

### 제24조 ('모름'과 '미발화'의 구분)
- **금지**: 전부 결측인 행의 합계를 0 으로 만드는 것(`sum(skipna=True)` 의 함정).
- **의무**: 관측된 항목 수를 함께 기록하고, 관측이 0 이면 결과를 결측으로 둔다.
- **근거**: 문서 파싱에 실패한 법인이 '사실이 하나도 없는 법인'과 같은 척도로 표준화되어,
  점수가 **사실 건수가 아니라 데이터 커버리지의 함수**가 됐다.

### 제25조 (다중 소스 플래그의 상호 소거 방지)
- **금지**: 소스별로 행을 따로 만들고 as-of 결합으로 **최근 1행만** 붙이는 것.
  그 행에 없는 플래그는 NaN → 0 → **해제**가 된다.
- **의무**: 이벤트마다 **유효기간**을 부여하고 만료 시점에 명시적 off 행을 생성해,
  어느 시점을 집어도 **한 행 안에서 모든 플래그의 상태가 완결**되게 한다.
- **근거**: 감사의견 '의견거절' 종목이 10년 백테 전 구간에서 **한 번도 제외되지 않았다.**
  재무 행은 매 분기 무조건 생성되므로, 연 1회짜리 감사의견 플래그가 다음 분기보고서
  접수 순간 영구히 덮였다. 유일한 하드 제외 경로가 통째로 무력했다.
- **검증**: 계약검정 — 서로 다른 접수일의 두 소스 플래그가 **동시에 유지**되고, 유효기간 경과 후 해제됨.

### 제26조 (결측이 순위에 미치는 영향의 중립화)
- **금지**: 한쪽 축이 결측일 때 그 축만 0 으로 채우고 나머지 가중치를 그대로 두는 것.
- **의무**: 가중치 재배분은 **양방향 대칭**으로 한다.
- **의무**: 재배분 후 **가용성 그룹별로 기간 안에서 재표준화**한다.
- **검증**: 계약검정을 "점수가 NaN 이 아닌가"가 아니라 **"상위 N 편입률이 커버리지 비율과
  같은가"** 로 한다.
- **근거**: 한 축 결측 행은 합성 후 분산이 절반이 되고, 상위 N 선정은 꼬리에서 일어나므로
  NaN 이 아닌데도 구조적으로 밀렸다(실측 과대선택 2.5배, 커버리지 50% 에서는 상위 30 에
  한 종목도 못 들어감). 기존 계약검정은 NaN 만 봐서 이 붕괴를 잡지 못했다.

### 제27조 (강제 순위 배정)
- **금지**: '최하위 순위 배정'을 **값 대입**으로 구현하는 것.
- **의무**: 표준화 **이후** 셀 내 최하위 z 로 배정한다.
- **근거**: 대입값의 범위(전 기간/분기)와 표준화 범위(분기×섹터)가 어긋나, 분모가 깨진
  종목 1개 때문에 같은 셀 39종목 전원의 점수 표준편차가 1.01 → 0.36 으로 압축됐다.

---

## 제7장 통계·성과 규약

### 제28조 (지표 정의의 정확성)
- **의무**: Sortino 하방편차는 **목표 대비 제곱평균** `√(mean(min(r−T,0)²))` 이다.
  '음수 수익률들의 자기 평균 대비 표본표준편차'가 아니다.
- **의무**: IC-IR 은 `mean(IC)/std(IC)` 다. `×√n` 을 곱한 값은 **t통계량**이며 별도 컬럼으로 표기한다.
- **근거**: 잘못된 하방편차가 정의값 3.88 을 393 으로 만들었다(손실의 크기가 아니라
  균일함을 보상). IC-IR 표기 오류는 분기 40개에서 6.32배 부풀린 값을 읽게 만든다.

### 제29조 (수익률의 물리적 한계)
- **의무**: 분기 수익률을 −1 로 클램프한다. 최대낙폭을 −1 로 클램프한다.
- **의무**: 자본곡선이 **경로 중간에** 0 이하를 통과하면 파산으로 처리한다(최종값만 보지 않는다).
- **근거**: `1+r < 0` 이 두 번 나오면 누적곱이 부호를 두 번 뒤집어 파산 경로가 양의 자본으로
  되살아났다. 정의상 불가능한 MDD −112% 가 표에 인쇄됐다.

### 제30조 (측정 불가 기간의 배제)
- **의무**: 전방수익률을 측정할 수 없는 기간(마지막 리밸일 등)을 성과 통계에서 **일관되게** 제외한다.
  수익률 계열·초과수익·유의성 검정·평균 종목수·평균 회전율이 **전부 같은 표본**을 써야 한다.
- **금지**: 측정 불가 기간을 '수익률 0' 으로 성과 시계열에 넣는 것.
- **근거**: 연수·변동성·승률 분모가 조용히 왜곡됐고, 유의성 판정이 성과표와 다른 표본
  (39 vs 40분기)에서 나왔다.

### 제31조 (거래비용의 대칭성)
- **의무**: 비용 계산에 쓰는 유동성 정보는 **매수·매도 종목 모두**에 대해 조회 가능해야 한다.
- **금지**: 신규 편입 종목 목록으로만 유동성 맵을 만들고 매도 종목을 조회하는 것.
- **근거**: 전량 매도 종목이 유동성 맵에 없어 '데이터 없음' 폴백(일괄 2%)을 탔다.
  매도 슬리피지가 매수의 2.4배로 매겨져 **연 3~4%p 유령 비용**이 붙었고, 회전율이 다른
  실험 팔이 부당한 벌점을 받아 판정이 뒤집힐 수 있었다.

### 제32조 (회귀의 자유도)
- **의무**: 횡단면 회귀의 자유도 하한을 **관측수 ≥ K × 파라미터수** 비율로 잡는다(K ≥ 5).
- **금지**: `파라미터수 + 상수` 형태의 절대 하한.
- **의무**: 자유도가 부족하면 **파라미터를 줄이는 사다리**를 타되, 전 단계 실패 시 결측으로 둔다.
  직교화하지 않은 원신호를 쓰지 않는다.
- **의무**: 셀별 (관측수/파라미터수) 비를 로그로 노출한다.
- **근거**: 파라미터 14개에 관측 17개면 통과하는 가드였고, 그때 잔차와 원신호의 상관은 0.42 였다.
  나머지 58% 가 규모·모멘텀·섹터의 결정적 함수 — **위장된 사이즈 베팅**이 최종 점수의 50% 를 차지했다.

---

## 제8장 실패·진단 규약

### 제33조 (조용한 실패 금지)
- **금지**: `except Exception: pass`.
- **의무**: 모든 예외 흡수 지점은 **무엇이 실패했고 그 결과 무엇이 결측이 되는지** 로그로 남긴다.
- **근거**: 시총 폴백 경로가 로그 한 줄 없이 죽어, 원인이 사용자 설정 문제로 오인됐다.

### 제34조 (진단 로그의 정확성)
- **금지**: 실패 방향을 **잘못** 기술하는 것.
- **적용례**: "구분하지 못해 과대계상될 수 있습니다"라고만 쓰고, 절반의 이벤트가
  **구조적으로 영구 0** 이라는 사실을 숨긴 로그가 실제로 있었다.
- **의무**: 경고문은 사용자가 **취할 수 있는 행동**을 포함한다.

### 제35조 (죽은 코드 금지)
- **금지**: 도달할 수 없는 분기를 안전망이라고 부르는 것.
- **의무**: 명세의 핵심 규정을 구현한 함수는 **실제로 호출되는 경로**에 있어야 한다.
- **근거 ①**: 상장폐지 −100% 안전망이 상류 필터 때문에 도달 불가였다(2중 방어가 아니라 1중).
- **근거 ②**: '완료형 사실만 센다'는 핵심 필터가 상수 조건 뒤에 있어 **한 번도 실행되지 않았다.**
  게다가 두 단어 패턴은 토큰 덤프 문자열에서 **원리상 매칭 불가**라 해당 이벤트가 영구 0,
  단일 토큰 패턴은 상시 발화(실측 67%)였다.
- **검증**: 정기적으로 도달 불가 정의를 기계적으로 탐지한다.

### 제36조 (부가 기능이 본 실행을 막지 않는다)
- **의무**: 육안 검증용 표·흡수 스캔·리포트 출력의 실패가 데이터 파이프라인을 끊지 않는다.
- **근거**: 표 출력 함수가 결측값 포맷에서 예외를 던져 스테이지가 접혔고,
  그 결과 **원문을 전부 받아 놓고도 신호 축 하나가 통째로 비활성화**된 채 백테스트가 완주했다.

### 제37조 (스테이지 스킵의 실질)
- **금지**: 컨텍스트 매니저로 본문 실행을 막을 수 있다고 가정하는 것. 파이썬에서는 불가능하다.
- **의무**: 스킵은 **호출부에서 명시적 분기**로 구현한다.
- **의무**: 스킵 경로도 실행 경로와 **동일한 예외 가드**를 갖는다.
- **근거**: 축을 껐다고 로그에 찍히는데 계산이 다 돌아가고 값까지 반영됐다.
  스킵 경로에서는 `critical=False` 가 무력화돼, 데이터가 부실할 때(=본문이 던질 확률이
  가장 높을 때) 가드가 사라졌다.

### 제38조 (dtype 안정성)
- **의무**: 결합 키는 사용 직전에 **문자열로 고정**한다.
- **근거**: 대용량 프레임의 다운캐스팅이 종목코드를 category dtype 으로 바꿔
  `merge_asof(by=...)` 가 죽었고, 시총이 100% 결측이 되어 실행 전체가 중단됐다.
  스냅샷 캐시가 있는 환경에서는 재현되지 않아 리허설·스모크를 전부 통과했다.

### 제39조 (스키마 변화 내성)
- **의무**: 공용 캐시에서 읽은 프레임은 **과거 버전 스키마**일 수 있다고 가정한다.
  컬럼 부재·전체 결측에서 예외가 나지 않아야 한다.

---

## 제9장 검증 규약

### 제40조 (검증은 코드로만)
- **금지**: 주석·문서·관례를 근거로 규약 준수를 주장하는 것.
- **의무**: 모든 조항은 **실제 데이터를 만들어 함수를 돌리는** 계약검정을 갖는다.
- **의무**: 계약 위반 시 파이프라인을 즉시 중단한다(우회 스위치를 만들지 않는다).

### 제41조 (검정 설계의 함정)
계약검정 자체가 결함을 놓친 사례가 반복됐다. 다음을 강제한다.
1. **NaN 유무가 아니라 결과 분포를 본다.** (제26조)
2. **미래 데이터를 바꿔 과거 결과가 변하는지 본다.** (제14조)
3. **별도 프로세스로 재현성을 본다.** (제18조)
4. **합성 데이터의 규모가 실제 조건을 만족하는지 본다.**
   합성 종목 수가 유니버스 상한보다 작으면 '유니버스 이탈'이 구조적으로 발생하지 않아
   해당 결함군을 영원히 못 잡는다.
5. **플래그가 1이 되는 경로 자체를 검사한다.** 'EXCLUDE=1 이면 편입 0건'만 보면
   플래그가 영영 1이 되지 않는 결함을 통과시킨다. (제25조)

### 제42조 (검증 계층)
1. **계약 자동검정** — 조항별 1개 이상
2. **실경로 리허설** — 수집·정제 함수를 가짜 네트워크로 4모드(정상/빈응답/깨진응답/컬럼누락) 구동
3. **합성 엔드투엔드 스모크** — 신호를 심어 IC 가 검출되고, 잡음에서는 ~0 이 나오는지 확인

### 제43조 (검증 하네스의 분리)
- **의무**: 검증 코드는 산출물 본체와 **별도 파일**로 둔다. 본체는 하네스 없이 완전히 동작한다.
- **근거**: 사용자 산출물에 개발용 검증층이 섞이면 부피와 실행시간이 불필요하게 커진다.

---

## 제10장 산출물·사용자 인터페이스 규약

### 제44조 (키 입력부)
- **의무**: API 키·계정 정보는 **파일 최상단 한 곳**에 모으고, 각 항목에 **발급처 URL 과
  소요 시간·전제조건**을 병기한다.
- **적용례**: "KRX Open API 는 엔드포인트별 이용신청이 따로 필요하고 승인에 하루 정도 걸린다.
  키 발급만으로는 즉시 사용할 수 없다" — 실제로 겪은 함정은 반드시 적는다.
- **금지**: 사용하지 않는 키 항목을 입력부에 남겨두는 것.

### 제45조 (실행 시간 예측)
- **의무**: 대량 수집 전에 **필요 호출 수와 예상 소요시간을 표로 출력**한다.
- **의무**: 벽시계 예산을 두고, 예산 초과 시 **전수 커버리지를 확보하는 단계는 반드시 끝내고**
  정밀화 단계만 자른다.
- **금지**: "며칠에 걸쳐 완성됩니다"를 정상 시나리오로 설계하는 것.

### 제46조 (비교 팔)
- **의무**: 유니버스 크기 민감도(하위 500 / 1000 / 2000)를 강건성 항목으로 병기한다.
  패널을 재사용하므로 추가 수집 비용은 0 이다.

### 제47조 (코드 구조)
- **금지**: 다른 프로젝트의 인프라를 **그대로 복사**해 넣는 것. 아이디어와 인프라 차용은
  허용하되, 사용하지 않는 정의는 반입하지 않는다.
- **의무**: 같은 골격이 3벌 이상 반복되면 **스펙 테이블 + 실행기 1개**로 접는다.
  (적용례: 같은 순서를 따르는 API 어댑터군, 같은 형태의 표 출력군)
- **금지**: 진짜 반복이 아닌 것을 억지로 접는 것. 가독성을 해치고 결함이 들어간다.
  반복이 없으면 **없다고 보고하고 줄이지 않는다.**
- **의무**: 도달 불가 정의는 반입 즉시 제거한다.

---

## 부록 A. 재발 금지 결함 목록 (전수)

| # | 분류 | 결함 | 조항 |
|---|---|---|---|
| 1 | 순서 | 유니버스 확정 전 전 종목 일봉 수집 (5,398종목) | 1 |
| 2 | 순서 | 유니버스 확정 전 전 법인 재무 수집 (3,981사) | 1 |
| 3 | 호출 | 응답의 전기·전전기 값을 버리고 재호출 (×3) | 2 |
| 4 | 호출 | OFS 먼저 → CFS 재호출 (×1.8) | 2 |
| 5 | 호출 | 힌트값을 상한처럼 사용 → "12일 걸림" 오안내 | 3 |
| 6 | 호출 | 파라미터 오류(021)를 한도초과(020)로 처리 → 예산 원장 영구 오염 | 3 |
| 7 | 호출 | 죽은 소스(pykrx 0성공)에 1,322회 헛호출 | 4 |
| 8 | 호출 | 폐지 종목에 yfinance `.KS`/`.KQ` 2회씩 | 4 |
| 9 | 호출 | 앞 구간 결손 시 10년치 전체 재수집 (1,049종목) | 5 |
| 10 | 호출 | 음성캐시 시계를 백테 종료일로 잡아 영구 블랙리스트 | 5-2 |
| 11 | 호출 | DART 빈 응답 음성캐시 부재 → 매 실행 94% 재호출 | 5-2 |
| 12 | 순서 | 인증 전 워밍업 → 로그인 성공에도 시총 스냅샷 전부 스킵 | 6 |
| 13 | 캐시 | 공용 일봉 테이블을 요청 구간으로 잘라 되씀 | 7 |
| 14 | 캐시 | `_backup` 무한 증식 | 7 |
| 15 | 캐시 | 전략별 볼트 루트가 달라 `_shared` 미공유 (709만 행 재수집) | 8 |
| 16 | 캐시 | 드라이브 마운트 실패 → 조용한 로컬 폴백 → 전량 재수집 | 9 |
| 17 | 캐시 | 일시적 IO 오류에 `.corrupt` 개명 → 영구 고아 | 10 |
| 18 | 캐시 | 백업 실패 시 rev 파일에만 저장, 아무도 읽지 않음 | 10 |
| 19 | 캐시 | 레거시 uid 가 행 위치 기반 → 컴팩션마다 인덱스 증식 | 11 |
| 20 | 캐시 | 흡수 스캔이 볼트 자신을 스캔 + `has()` O(n²) → 사실상 정지 | 12 |
| 21 | 캐시 | `_mount_drive` 가 탐색 경로를 쓰기 루트 후보로 미참조 | 9 |
| 22 | 캐시 | 파싱 실패 문서를 매 실행 재다운로드 | 5-2 |
| 23 | PIT | 시총 스냅샷이 리밸일 당일 종가인데 리밸일로 라벨링 | 13 |
| 24 | PIT | D1 최종 표준화 셀이 달력연도 | 14 |
| 25 | PIT | D2 윈저 경계·강제최하위가 전 기간 백분위 | 14 |
| 26 | PIT | 셀 키 컬럼 부재 시 조용한 폴백 → 수정이 무동작 | 14-2 |
| 27 | PIT | 구조변화 플래그 ±365일 양방향 매칭 | 15 |
| 28 | PIT | 편입 자격을 전방수익률 결측 여부로 조건 지음 | 16 |
| 29 | PIT | 정정공시가 원본을 프레임에서 대체 | 17 |
| 30 | PIT | 폴백 분류기가 파이썬 `hash()` → 실행마다 보유종목 변화 | 18 |
| 31 | PIT | 재무 knowledge_date 에 T+1 미적용 | 13 |
| 32 | 생존 | 거래정지 → 폐지 경로의 −100% 미계상 | 19 |
| 33 | 생존 | 정상 폐지(합병 등)에 −100% | 20 |
| 34 | 생존 | 정지 후 재개를 폐지로 오인 → 다년 수익률이 1분기 자리에 | 20-2 |
| 35 | 생존 | 폐지목록의 상장일을 버리고 첫 관측일로 날조 | 21 |
| 36 | 생존 | 리츠 부분문자열이 메리츠·블리츠 계열 보통주 제외 | 22 |
| 37 | 생존 | 시장 필터 부재 → KONEX·수익증권·선박투자회사 편입 | 22 |
| 38 | 결측 | NOA 에서 현금 결측을 0 으로 대체 | 23 |
| 39 | 결측 | 전부 결측인 행의 합계가 0 이 됨 | 24 |
| 40 | 결측 | 배제 플래그가 소스별로 상호 소거 (감사의견 10년간 미발동) | 25 |
| 41 | 결측 | 한 축 결측 시 분산 축소로 상위 N 에서 구조적 배제 (2.5배) | 26 |
| 42 | 결측 | 강제 최하위를 값으로 대입 → 같은 셀 전원 압축 | 27 |
| 43 | 통계 | Sortino 하방편차 정의 오류 (3.88 → 393) | 28 |
| 44 | 통계 | t통계량을 IC-IR 로 표기 (√n 배 부풀림) | 28 |
| 45 | 통계 | 자본곡선 0 통과 시 MDD −112% 출력 | 29 |
| 46 | 통계 | 측정 불가 분기를 수익률 0 으로 삽입 | 30 |
| 47 | 통계 | 유의성 판정이 성과표와 다른 표본 (39 vs 40분기) | 30 |
| 48 | 통계 | 매도 슬리피지가 항상 2% 폴백 (연 3~4%p 유령 비용) | 31 |
| 49 | 통계 | 직교화 자유도 가드가 p+3 → 잔차 58% 가 적합오차 | 32 |
| 50 | 진단 | `except Exception: pass` 로 시총 경로 소멸 | 33 |
| 51 | 진단 | 실패 방향을 반대로 기술한 경고문 | 34 |
| 52 | 진단 | 상장폐지 안전망이 도달 불가 사문 | 35 |
| 53 | 진단 | 완료형 필터가 상수 조건 뒤에서 한 번도 미실행 | 35 |
| 54 | 진단 | 표 출력 예외가 신호 축 전체를 비활성화 | 36 |
| 55 | 진단 | 스킵 경로에서 `critical=False` 무력화 | 37 |
| 56 | 진단 | category dtype 이 결합을 죽여 시총 100% 결측 | 38 |
| 57 | 진단 | 과거 스키마 캐시에서 `int(NaN)` 예외 | 39 |
| 58 | 프로세스 | 적용되지 않은 수정을 적용했다고 커밋 | 0-2 |

---

## 부록 B. 명세서 재작성 시 삭제할 것

1. **"~하는 것이 좋다" 형태의 권고** — 전부 금지/의무로 바꾸거나 삭제.
2. **검증 방법이 없는 규정** — 계약검정을 붙이거나 삭제.
3. **단계 순서가 명시되지 않은 수집 지시** — 제1조의 3단계 깔때기에 배치.
4. **"콜드빌드는 며칠 걸린다"는 전제** — 제45조 위반. 12시간 내 완료를 설계 조건으로.
5. **하드 게이트로 쓰지 않기로 한 규칙의 잔재** — 가점과 하드 게이트를 문서 전체에서 명확히 분리.
6. **동일 내용의 중복 서술** — 조항 번호 상호참조로 대체.

---

## 부록 C. 명세서에 반드시 추가할 것

1. **제1장 전체** (파이프라인 3단계 깔때기) — 기존 명세에 없던 개념.
2. **각 조항의 계약검정 ID 매핑표.**
3. **실행 시간 예산표** — 단계별 예상 호출 수·소요시간, 초과 시 무엇을 자를지.
4. **캐시 계층 정의** — 로컬 / 자기 볼트 공용 / 타 전략 볼트 공용 / 전용, 각각의 읽기·쓰기 권한.
5. **결함 재발 시 판정 절차** — 부록 A 의 번호로 지목하고 해당 조항의 계약검정을 돌린다.
