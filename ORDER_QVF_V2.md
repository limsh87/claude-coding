# QVF-FUNNEL 개선 명령서 v2.0
## 깔때기 순서 강제 · 병목 해체 · 발생 에러 전수 대응

> **이 문서는 자기완결적입니다.** 다른 문서를 참조하지 마십시오.
> **대상**: `strategies/qvf_funnel_v1.py` (빌드 `qvf1.1.20260810.1951` · 16,067줄) 및
> 그 조각 파일 `build/q*.py` · 조립기 `tools/build_qvf.py`.
> **원칙**: 이 문서의 모든 수치는 **사용자 실환경 FULL 실행 로그(2026-08-11, Windows ·
> py3.14 · 16코어)에서 타임스탬프를 초 단위로 분해한 실측값**입니다. 추정치가 아닙니다.
> 추정으로 대체하지 말고, 고친 뒤 같은 방식으로 다시 재서 비교하십시오.

---

## 0. 실행자에게 — 이 문서를 읽는 방법

이 명세서는 세 가지를 요구합니다. **우선순위 순서대로입니다.**

| 순위 | 요구 | 이유 |
|---|---|---|
| **1** | §2 깔때기 순서 강제 | 지금 코드는 원칙을 **문서로만** 지키고 배선은 어긴다. 크래시 시 282초가 버려진다 |
| **2** | §3 병목 해체 | 실측 상위 3개가 전체의 **62.3%**. 그런데 셋 다 '범위'가 아니라 '연산' 문제다 |
| **3** | §4 에러 대응 | 13건 중 11건 수정·2건 미해결. 재발 방지가 목적 |

**가장 중요한 경고부터 말합니다.**

> ⚠ **"U-200 으로 좁히면 빨라진다"는 명제는 부분적으로만 참입니다.**
> 이미 좁혀서 얻은 것(PDF 34시간 → U-200, 상세조회 36분 → U-200)은 **끝났습니다**.
> 남은 병목 상위 3개(전체의 62.3%)는 **범위를 좁혀도 줄지 않습니다** — 연산 방식 문제입니다.
> 이 사실을 모르고 "더 좁히기"에 시간을 쓰면 아무것도 개선되지 않습니다. §3.0 을 먼저 읽으십시오.
>
> 그럼에도 §2 의 순서 강제는 **반드시** 해야 합니다. 이유는 속도가 아니라 **낭비 회피와
> 원칙의 검정 가능성**입니다(§2.3).

---

## 1. 실측 기준선 — 이 숫자를 기준으로 개선을 측정한다

### 1.1 스테이지별 실측 (FULL · 웜캐시 · L1.PANEL1 크래시까지 938.70초)

```
계층 스테이지        초      비고
─────────────────────────────────────────────────────────────────────
L0   VAULT          4.09
L0   CONTRACT       0.68    계약 Q1~Q14
L0   SPEC           4.55    명세 전수조사 32조항/272소검정
L0   SMOKE         20.80
L0   REHEARSAL     82.74    ★ 이 중 77.35초가 L1.DIS 와 완전 중복
L1   UNI            4.20
L1   CAL            0.55
L1   CAP            9.06
L1   PX            41.40
L1   DART         408.81    ★ 이 중 303.26초가 tidy_financials 단일 함수
L1   DIS           75.67    ★ 이 중 70.41초가 REHEARSAL 과 동일 연산
L1   FLOW           0.56
L1   RESEARCH     282.70    ★ 이 중 133.89초가 build_report_master
L1   AUDIT          0.71
L1   PANEL1         2.19    ✘ MergeError 크래시
─────────────────────────────────────────────────────────────────────
합계               938.70   (15.65분)
```

### 1.2 하위 연산 단위 실측 — 여기가 진짜 병목이다

| 순위 | 항목 | 초 | 전체 대비 | 범위를 좁히면 줄어드는가 |
|---|---|---|---|---|
| 1 | `tidy_financials` (6.3M행 → 93,363행) | **303.3** | 32.3% | ✘ **아니오** — 후보 2,572사는 U-1000 선정에 전부 필요 |
| 2 | 공시 이벤트 분류 **2회 중복** (77.4 + 70.4) | **147.8** | 15.7% | ✘ **아니오** — 같은 결과를 두 번 계산한다 |
| 3 | `build_report_master` (209,797행 병합) | **133.9** | 14.3% | △ **부분** — 저장은 전량, 연산은 U-200 (§3.3) |
| 4 | 네이버 리스트 스윕 (290페이지) | 116.4 | 12.4% | ✘ **아니오** — 날짜 페이지네이션. 종목별 조회는 **더 느리다** |
| 5 | DART Tier-1 배치 615회 | 77.6 | 8.3% | ✘ 이미 최적(100사/호출) |
| 6 | 한경 스윕 (1개 연도) | 18.5 | 2.0% | ✘ 위와 동일 |
| 7 | `build_analyst_ledger` | 10.6 | 1.1% | ○ 예 |
| | **상위 합계** | **807.9** | **86.1%** | |

**읽는 법**: 상위 3개(585.0초 · 62.3%) 중 **범위 축소로 줄어드는 것은 3번 하나뿐이며 그것도 부분적**입니다.
1번과 2번은 순수한 연산 설계 문제입니다.

---

## 2. 제1원칙 — 수집은 깔때기 뒤에 놓는다 (순서 강제)

### 2.1 현재 배선 — 원칙 위반 지점

```
L1.UNI → L1.CAL → L1.CAP → L1.PX → L1.DART → L1.DIS → L1.FLOW
       → L1.RESEARCH → L1.AUDIT → 【L1.PANEL1】 → L1.FACTS → L1.TONE → L2.PANEL2
                                    ↑
                            여기서 U-1000·U-200 이 처음 만들어진다
```

`L1.DIS`(75.7초)와 `L1.RESEARCH`(282.7초)와 `L1.AUDIT`(0.7초) — **합계 359.1초, 전체의
38.3%** — 가 U-200 이 존재하지도 않는 시점에서 실행됩니다.

### 2.2 소비 시점 검증 (코드 확인 완료 — 추정 아님)

`build_panel_pass1()` 이 `ctx` 에서 읽는 것 전부:
```
fin · shares · snaps_cap · snapshots · fq · px · sec
```
**`reports` 도 `dis` 도 읽지 않습니다.** 첫 소비 지점은:

| 산출물 | 생산 위치 | 첫 소비 위치 | 간격 |
|---|---|---|---|
| `ctx["dis"]` | L1.DIS (6번째) | `build_nonfin_panel` (L2.PANEL2) · L1.FACTS | 4 스테이지 뒤 |
| `ctx["reports"]` | L1.RESEARCH (8번째) | `audit_linkage`(L1.AUDIT) · L1.TONE | — |

→ **L1.DIS · L1.RESEARCH · L1.AUDIT 를 L1.PANEL1 뒤로 옮기는 것은 순수한 재배치이며
의존성 위반이 전혀 없습니다.**

### 2.3 왜 옮겨야 하는가 — 속도가 아니라 세 가지 다른 이유

1. **크래시 낭비 회피 (실측)**
   이번 실행은 L1.PANEL1 에서 죽었습니다. 그 전에 이미 **359.1초(6분)를 아무도 쓰지 않을
   데이터 수집에 태웠습니다.** 순서를 바꾸면 이 실행은 **6분이 아니라 0초**를 낭비합니다.
   콜드캐시라면 리서치 스윕만 **약 3시간**입니다.

2. **범위 축소가 비로소 가능해진다**
   U-200 을 모르면 `build_report_master`(133.9초)를 좁힐 수 없습니다. 순서를 바꿔야 §3.3 이
   실행 가능해집니다. **순서 변경은 §3.3 의 전제조건입니다.**

3. **원칙이 검정 가능해진다**
   지금은 "수집은 깔때기 뒤에"가 주석에만 있습니다. 순서를 바꾸면 **계약으로 강제**할 수
   있습니다(§2.5). 검정할 수 없는 원칙은 원칙이 아니라 희망입니다.

### 2.4 요구 배선

```
L1.UNI → L1.CAL → L1.CAP → L1.PX → L1.DART → L1.FLOW
       → 【L1.PANEL1】  ← U-1000 확정 → V/Q/F 축 → U-200 3변형 확정
       → L1.DIS        ← 이동. 수집은 전 시장이되 '가공'은 U-200 union (§3.2)
       → L1.RESEARCH   ← 이동. 리스트는 전 시장이되 '병합·원장'은 U-200 union (§3.3)
       → L1.AUDIT      ← 이동. 감사 대상도 U-200 union
       → L1.FACTS → L1.TONE → L2.PANEL2 → ...
```

**주의**: `ctx["need_codes"]`(U-200 3변형 합집합)는 현재 L1.FACTS 에서 만들어집니다.
이것을 **L1.PANEL1 끝으로 올려야** L1.DIS/L1.RESEARCH 가 쓸 수 있습니다.

### 2.5 강제 수단 — 계약을 추가할 것 (필수)

`build/q71_specaudit.py` 에 조항을 추가하십시오. **이 검정이 없으면 다음 세션이 다시 순서를
되돌립니다.**

```
@spec_clause("§순서", "수집은 깔때기 뒤에 — U-200 이전 스테이지가 리포트·공시를 만지지 않는다")
  [1] main() 소스에서 스테이지 등록 순서를 추출해 L1.PANEL1 의 인덱스가
      L1.DIS · L1.RESEARCH · L1.AUDIT 보다 '작은지' 검정한다
  [2] build_panel_pass1 의 co_names 에 'reports'/'dis'/'links'/'analysts' 가
      등장하지 않는지 검정한다 (미래 세션이 의존성을 새로 만드는 것을 막는다)
  [3] 검정력 확인: 순서를 뒤집은 가짜 리스트를 넣으면 이 검정이 실패해야 한다
```

---

## 3. 병목 해체 — 측정된 것만, 측정된 만큼

### 3.0 먼저: 좁혀도 줄지 않는 것을 확정한다 (헛수고 방지)

다음은 **범위 축소로 개선되지 않습니다.** 시도하지 마십시오.

| 대상 | 왜 안 되는가 |
|---|---|
| 애널리스트 **리스트** 스윕 (134.9초) | 한경·네이버 리서치 목록은 **날짜 페이지네이션**이다. "이 200종목만 주세요"라는 질의가 없다. 종목별 조회(`itemCode=`)로 바꾸면 U-200 합집합 ≈ 1,000~2,000종목 × 2페이지 = 4,000요청 / 2.5qps = **약 27분** — 지금(2.2분)보다 **12배 느리다** |
| DART **공시목록** 스윕 | `list.json` 은 날짜 구간 질의다. 회사별로 쪼개면 126개월 × 종목수 만큼 호출이 폭증한다 |
| `tidy_financials` (303.3초) | 후보 2,572사는 **U-1000 을 정하기 위해** 전부 필요하다. 재무(자기자본)가 없으면 자본잠식 게이트를 못 돌린다. 순환 의존이다 |
| DART Tier-1 (77.6초) | 615회로 2,897사. 100사/호출이라 이미 최적이다 |

**이미 좁혀서 끝난 것** (다시 손대지 마십시오):
PDF 다운로드(19.8만건/34시간 → U-200), 네이버 상세조회(5,434건/36분 → U-200),
사업보고서 본문(L1.FACTS → U-200), DART 섹션 텍스트(L1.TONE → U-200).

### 3.1 [최우선] 공시 이벤트 분류 2회 중복 — 147.8초 (15.7%) · 난이도 하

**증상** (로그 원문, 결과가 **완전히 동일**):
```
[00:32.91] L0.REHEARSAL  캐시에서 QVF 공시목록 921,010행 · 126개월 재사용
[01:50.26] L0.REHEARSAL  ✔ QVF 공시목록 921,010건 — supply_contract=39,257, cb_issue=20,822, ...
[09:39.88] L1.DIS        캐시에서 QVF 공시목록 921,010행 · 126개월 재사용
[10:50.29] L1.DIS        ✔ QVF 공시목록 921,010건 — supply_contract=39,257, cb_issue=20,822, ...
```

**원인**: 리허설이 `fetch_disclosures_qvf()` 를 **실물 실행**하고, L1.DIS 가 같은 함수를 다시
호출한다. 921,010행에 이벤트 분류 정규식을 두 번 돌린다.

**요구 조치 (둘 다)**:
1. **분류 결과를 파생 테이블로 캐시한다.** `dart_disclosures_qvf` 는 원본 목록이고, 이벤트
   태그(`supply_contract`·`cb_issue`·…)는 파생물이다. `dart_disclosure_events` 로 저장하고
   원본 행수·해시가 같으면 재분류하지 않는다.
2. **리허설은 전량을 돌리지 않는다.** 리허설의 목적은 "빈 응답·비정상 응답에 죽지 않는가"이지
   "전 코퍼스를 분류하는가"가 아니다. **상한 5,000행 표본**으로 충분하며, 그 사실을 로그에
   명시한다.

**기대 효과**: 147.8초 → 약 5초. **전체 실행의 15.2% 단축.**

### 3.2 [최우선] `tidy_financials` 303.3초 (32.3%) · 난이도 중

**증상**: 6,318,467행(Tier-1 1,224,295 + Tier-2 5,094,172) 입력 → 93,363행 출력에 **5분 3초**.
출력 1행당 입력 68행. 이 함수 하나가 전체의 3분의 1이다.

**원인 가설 (반드시 프로파일로 확인할 것 — 추정으로 고치지 마십시오)**:
- 계정 매핑이 `ACCOUNT_PATTERNS` 정규식을 **행마다** 돌린다 → 계정명은 카디널리티가 낮다
  (수천 종). **고유 계정명에 대해 한 번만 매핑하고 `map()` 으로 되돌리면** 6.3M회가 수천 회가 된다
- 누적→분기 차분에서 `groupby.apply` 또는 파이썬 루프
- 반복적인 `pivot`/`merge`

**요구 조치**:
1. `cProfile` 또는 구간 타이머로 **함수 내부를 먼저 분해해 로그로 출력**하십시오. 어느 줄이
   303초를 쓰는지 모르는 채 고치면 안 됩니다.
2. 고친 뒤 **출력이 비트 단위로 동일한지** 검정하십시오 (93,363행 · 같은 컬럼 · 같은 값).
   성능 최적화가 값을 바꾸면 그건 최적화가 아니라 사고입니다.
3. 정제 결과(`dart_financials_tidy`)를 **캐시 테이블로 저장**하고, 원본 행수가 같으면
   재정제하지 않도록 하십시오. 재실행마다 5분을 다시 태울 이유가 없습니다.

**기대 효과**: 보수적으로 303초 → 60초 이하. **전체의 25.9% 단축.**

### 3.3 [중요] `build_report_master` 133.9초 — 저장은 전량, 연산은 U-200

**증상**: 209,797행 병합에 133.9초. 그런데 ΔTONE 이 실제로 쓰는 것은 **U-200 합집합 종목의
리포트뿐**입니다.

**주의 — 여기서 흔한 실수**:
> `research_report_master` 는 **공용 인덱스(shared) 자산**입니다. 다른 전략이 재사용합니다.
> 이것을 U-200 으로 잘라서 저장하면 **절대 1원칙(캐시 재활용)을 훼손**합니다.

**요구 조치 — 저장과 연산을 분리하십시오**:
```
· 수집·저장: 지금처럼 전 시장 전량 → _shared/table/research_report_master  (변경 없음)
· 병합·정규화·원장·감사: ctx["need_codes"](U-200 합집합)로 필터한 뒤 수행
```
즉 `build_report_master(frames, sec)` 에 `codes=` 인자를 추가하되, **캐시 기록은 전량으로
유지**하고 반환 프레임만 좁힙니다.

**기대 효과**: 133.9 + 10.6 + 0.7 = 145.2초 → U-200 비중(실측 필요, 대략 1/10~1/5)에 비례.
보수적으로 **약 100초 단축**. §2.4 순서 변경이 전제조건입니다.

### 3.4 [부수] 콜드캐시 경로 — 진짜 큰 절감은 여기 있다

웜캐시 15.65분은 이미 짧습니다. **문제는 콜드캐시입니다.** 순서 변경(§2)이 여기서 결정적입니다.

| 항목 | 콜드 비용 | 순서 변경 후 |
|---|---|---|
| 리서치 리스트 10년 스윕 | 약 3시간 | 크래시 시 **0** (PANEL1 통과 후에만 실행) |
| 공시 126개월 스윕 | 수십 분 | 동일 |
| DART Tier-2 콜드빌드 | 19,154호출 ≈ 하루 | 변화 없음(구조상 필요) |

---

## 4. 발생 에러 전수 — 원인 · 상태 · 재발 방지

### 4.1 수정 완료 (11건) — 같은 부류를 다시 만들지 말 것

| # | 에러 | 원인 | 재발 방지 |
|---|---|---|---|
| 1 | `MergeError: dtype('O') vs CategoricalDtype` — **L1.PANEL1 즉사** | 캐시 parquet(`krx_ohlcv_daily` 7,055,639행)의 `code` 가 category. `merge_asof(by=)` 는 키 dtype 이 정확히 같아야 한다 | 캐시 관문 `QVFVault.get_table` 에서 `KEY_STR_COLS` 12종 강제(`_decat_keys`) · 전수조사 `§TM키` 가 **같은 입력으로 재현** |
| 2 | `TypeError: Invalid value for dtype 'float32'` — 계약 Q3 실패 | float32 열에 float64 `.loc` setitem. pandas 2 는 조용히 캐스팅, 3 은 예외. **하필 폐지 −100% 경로** | `fwd_ret` 을 float64 로 못박음 |
| 3 | `stack(dropna=)` ValueError — §3.2 유동성 게이트 전멸 | pandas 3 에서 제거된 인자 | melt 로 교체 |
| 4 | `datetime64[us]` vs `[ns]` MergeError | pandas 3 기본 해상도 변경 | `as_ts`/`as_ts_series` 에서 ns 고정 |
| 5 | **Q축 5지표 중 3개가 z 를 하나도 만들지 않음** | 3상태 `valid` 의 `<NA>` 를 `fillna(False)` 로 뭉갠 뒤 z 표본 조건으로 사용 → 표본 0 | 호출부 `True` + 헬퍼 하드닝. 전수조사 §5.3 이 '산출 행수'를 검정 |
| 6 | **§10.4 ② 가 매 실행 발동 → 거짓 폐기** | `paired_diff_test` 만 `hac_tstat` 을 `t, _se` 로 언패킹(다른 8곳은 `mu, t`) | `_mu, t` 로 수정 · 전수조사 §8.3 이 't≠mean' 검정 |
| 7 | X0 벤치마크 표 전부 nan | `qperf_stats` 키가 `'CAGR'` 인데 `'cagr'` 조회 | §구조 검정이 문자열 상수 대조 |
| 8 | `ADTV_MIN_KRW` 미정의 전역 | 함수 **본문 안**이라 조립기의 최상위 검사가 못 잡음 | §구조 검정이 `LOAD_GLOBAL` 전수 대조 |
| 9 | `to_code6` 가 **없는 티커를 생성** | 문자 섞인 코드에서 숫자만 뽑아 zfill: `00341A`(쌍용양회4우B) → `000341`(보통주). 우선주 폐지일이 보통주에 붙는다 | 순수 숫자 입력에만 zfill · 전수조사 §3.3b |
| 10 | "폐지목록 37% 탈락 = 생존자편향"이라는 **오경보** | 유형 분해 없이 비율만 경고 | 탈락 1,821건 = 신주인수권증서 865·수익증권 783·신주인수권증권 160·**주권 13**(전부 구형 우선주) → 유형별 표로 보고 |
| 11 | Tier-2 예상일수 `19154.0일` | 분모가 '오늘 잔여'(=1) | `DartQuota.daily_limit()` 신설 |

### 4.2 미해결 (2건) — 실환경 접근이 필요하다

| # | 항목 | 상태 |
|---|---|---|
| A | **pykrx import 실패** (`JSONDecodeError` · Windows cp949) | 자동 우회 코드 추가(재시도 동안만 `locale.getpreferredencoding` → UTF-8). **실환경 검증 불가**(개발 컨테이너는 리눅스). 사용자 환경에서 `PYTHONUTF8=1` 설정 후 재현 여부를 먼저 확인할 것. **살아나면 시총이 근사가 아니라 실측이 된다** — 현재 시총 88,898행이 전부 '주식수 이월 근사'다 |
| B | **KRX 마켓플레이스 로그인** | 로그인 응답은 정상인데 인증 호출이 데이터를 안 줌(bld 파라미터 변경 추정). 실계정·네트워크 필요 |

### 4.3 다른 세션이 발견한 미해결 4건 — 판단이 필요한 것들

| 조항 | 결함 | 실측 | 성격 |
|---|---|---|---|
| §3.4 | 정리매매가를 **손실일 때만** 인정 → 합병·공개매수 등 **상향 청산이 −100%** 로 계상 | 청산 종가 130(진입 100)인데 `fwd_ret = −1.00` | **교환관계.** 손실 조건이 있는 이유도 실재한다(폐지 직전 소스 드롭 시 마지막 정상가가 청산가로 둔갑 → '폐지=0% 손실'). 실데이터에서 각 경우의 건수를 세는 것이 유일한 판단 근거 |
| §5.4 | `Z_F` 가 외국인·기관 z 를 **각각 표준화 후 평균** — '합산 순매수의 z' 가 아니다 | 결합 z 대비 상관 0.747 · 상위 30 자카드 0.429 | 명세 판단 필요 |
| §6.3 | 60~80 미달 시 경고 없음(§7.4 는 경고함) | 배제율 90% → 20종목인데 무경고 | 단순 누락 |
| §10.2 | 귀속 분해가 산문뿐, 표로 계산하지 않음 | — | 단순 누락 |

> ⚠ **§3.4 를 고치려면 `L0.SPEC` 검정도 같이 고쳐야 합니다.** 전수조사 §3.4 의 소검정
> "D 마지막 관측가가 '이익' → −100%" 는 **명세가 아니라 현재의 보수적 동작을 사전등록으로
> 못박은 것**입니다. 상향 청산을 인정하도록 바꾸면 그 검정이 실패하는데, 그건 회귀가 아니라
> 의도된 변경입니다. **검정을 먼저 고치고 코드를 고치십시오** — 반대로 하면 게이트가 막습니다.

---

## 5. 환경 계약 — 이것을 어기면 사용자 환경에서만 죽는다

개발 환경(리눅스 · py3.11 · pandas 3.0.5)과 사용자 환경(Windows · py3.14)이 다릅니다.
**"내 환경에서는 통과했다"는 근거가 되지 않습니다.** 아래는 실제로 사용자 환경에서만 터진
것들의 일반화입니다.

```
[1] merge/merge_asof 결합키
    · dtype 이 정확히 같아야 한다 (object vs category → MergeError)
    · datetime 은 해상도까지 같아야 한다 (us vs ns → MergeError)
    · 캐시에서 온 프레임은 '무엇이든 들어있을 수 있다'고 가정하라
[2] .loc / .iloc setitem
    · pandas 3 은 dtype 축소를 예외로 만든다. 대상 열의 dtype 을 먼저 확정하라
[3] 삭제·변경된 인자
    · stack(dropna=), fillna(method=), append 등. 같은 저장소 안에 이미 우회한 선례가
      있으면(p_x_customs 의 melt) 그것을 따르라
[4] 인코딩
    · Windows 기본이 cp949 다. 파일 입출력에 encoding 을 명시하라
```

---

## 6. 검증 계약 — 무엇을 통과해야 '됐다'인가

**4종 전부 통과해야 합니다. 하나라도 빠지면 완료가 아닙니다.**

```bash
python3 tools/build_qvf.py        # 조립 + 문법·중복정의·최상위 정의전참조 검사
python3 tools/audit_spec_qvf.py   # 명세 전수조사 (현재 32조항 · 272소검정)
python3 tools/smoke_qvf.py SMOKE  # 파일 실행 (계약 → L0.SPEC → 스모크 → 리허설)
# 원셀 실행 시뮬레이션 — __file__ 없이 exec. Colab 을 죽였던 버그의 유일한 재현 경로:
#   compile(src, "<ipython-input-1-abcdef>", "exec") + exec(code, {"__name__": "__main__"})
```

### 6.1 성능 개선의 검증 — 별도 요구

성능을 고쳤다면 **고치기 전/후 같은 방식으로 재서 표로 제시**하십시오.
"빨라졌습니다"는 보고가 아닙니다. `303.3초 → N초` 형태여야 합니다.
그리고 **출력이 동일한지 반드시 검정**하십시오(행수·컬럼·값). 값이 바뀌었으면 최적화가
아니라 사고입니다.

### 6.2 새 검정을 쓸 때 지킬 것 두 가지

1. **'검정력 확인' 소검정을 같이 넣으십시오.** 실패하는 입력을 만들 수 없는 검정은 통과해도
   아무것도 보장하지 않습니다. 이 프로젝트에서 계약 8개가 그 상태로 ✔ 를 찍고 있었습니다.
2. **전역을 만졌으면 `_SPEC_GUARDED` 에 이름을 넣으십시오.** 안 넣으면 검증층이 본 실행의
   입력을 바꿉니다.

---

## 7. 하지 말 것 — 이 프로젝트에서 반복된 실패 패턴

| 금지 | 이 프로젝트에서 실제로 벌어진 일 |
|---|---|
| **실패 시 작업을 넓히기** | 시총 소스가 죽자 "전 종목 일봉을 받습니다"로 확대 → 후보 3,100 → 5,398 로 폭주. **실패는 작업을 좁혀야 한다** |
| **못 물어본 것을 '없다'로 기록** | DART 예산 소진분을 "데이터 없음"으로 공용 캐시에 영구 기록 |
| **결측을 0(중립)으로 채운 뒤 z** | 순서를 바꾸면 '리포트 보유' 자체가 신호가 된다 |
| **'통과'를 근거로 삼기** | 계약 8개가 검정력 0 으로 통과 중이었다. TONE 축은 CV 0.77 인데 진짜부호 상관 −0.008 이었다 |
| **결과를 보고 임계·부호 조정** | 사전등록 위반. 부호가 역전되면 **뒤집지 말고** '검증 실패'로 보고할 것 |
| **성능 최적화가 값을 바꾸는 것** | §6.1 |
| **캐시 삭제·덮어쓰기** | 절대 1원칙. `read_parquet_safe` 가 드라이브 placeholder 를 `.corrupt` 로 개명해 폐지종목 캐시를 영구 소실시킨 전례가 있다 |

---

## 8. 작업 순서 요약 (이대로 진행할 것)

```
[1] §2 순서 강제           L1.DIS · L1.RESEARCH · L1.AUDIT 를 L1.PANEL1 뒤로
                           need_codes 생성을 L1.PANEL1 끝으로 이동
                           §순서 계약 추가 (검정력 확인 포함)
     ★ 중간 보고: 스테이지 순서표 + 계약 통과 여부

[2] §3.1 공시 분류 중복 제거   파생 테이블 캐시 + 리허설 표본화        기대 −147.8초
     ★ 중간 보고: 전/후 초 + 이벤트 건수 동일성 확인

[3] §3.2 tidy_financials       ① 프로파일로 내부 분해 → 로그 출력
                               ② 고침 ③ 출력 동일성 검정 ④ 결과 캐시   기대 −240초
     ★ 중간 보고: 프로파일 표 (어느 줄이 몇 초인지)

[4] §3.3 리포트 병합 범위      저장 전량 · 연산 U-200 (codes= 인자)     기대 −100초
     ★ 중간 보고: U-200 합집합 종목수 · 전/후 초

[5] §4.3 미해결 4건            §6.3·§10.2 는 즉시 · §3.4·§5.4 는 판단 요청 후

[6] §6 검증 4종 + 성능 전/후 표
```

**기대 총 효과**: 938.7초 → 약 450초 (웜캐시 기준 **52% 단축**).
콜드캐시·크래시 시나리오에서는 순서 변경만으로 **최대 3시간 낭비 회피**.

---

## 부록 A. 현재 상태 (전달 시점)

```
브랜치   claude/qvf-funnel-v1-handover-gsv22l
커밋     49a2e2e  (v1.2 — 카테고리 병합키 수정 + TONE-MEASURE v1.0)
빌드     qvf1.1.20260810.1951 · 16,067줄 · 809.6KB
PR       #23 (draft, mergeable_state: clean)
검증     4종 전부 통과 (pandas 3.0.5 / py3.11)
전수조사 32조항 · 272소검정 · 실패 0
```

**성과 수치에 대한 경고**: Q축 5지표화(§4.1-5)와 §10.4 ② 수정(§4.1-6)이 **선정 결과와 최종
판정을 바꿉니다.** 그 이전에 산출된 성과 수치는 전부 무효이며 인용하면 안 됩니다.
FULL 재완주가 필요합니다.

---

*본 문서와 코드는 전략 설계·검증용이며 투자자문이 아닙니다.*
