# ARC-NCQ v1.0 — New Coverage × Qualitative Shift

소형주 「신규 애널리스트 커버리지 × 보고서 텍스트 질적 변화」 탐지 전략. 명세서 ARC-NCQ v1.0 구현체.

> 시총 하위권 소형주에 **처음으로 리서치 보고서가 붙는 순간**은, 커버리지를 정당화할 사건이
> 이미 일어났다는 신호다. 그러나 스몰캡 리포트의 상당수는 실질 변화 없는 홍보성 보고서다.
> 그래서 두 단계로 압축한다:
> ① 신규 커버리지 이벤트 = 1차 필터 (수만 건 → 수천 건)
> ② 보고서 본문의 '구조적 성장' 밀도를 **같은 달 이벤트 풀 안에서 상대비교** = 2차 압축

---

## 1. 실행 파일

`strategies/arc_ncq_v1_new_coverage_quality.py` — 이 파일 **하나로 완결**된다.
Colab / JupyterLab 한 셀에 붙여넣거나 `python arc_ncq_v1_new_coverage_quality.py` 로 실행하면
**백테스트 → 성과검증 → 강건성검사 → 해석표 → 진단 9종**까지 전부 로그에 출력된다.

### 처음 실행하는 순서

1. 파일 상단 「⚙️ 사용자 설정」 블록만 채운다 (전부 비워도 실행된다).
2. `RUN_MODE = "SMOKE"` 로 한 번 돌린다 — 네트워크·키 없이 10초, 전 출력물 예행연습.
3. `RUN_MODE = "FULL"` 로 바꿔 실데이터 실행.

| `RUN_MODE` | 동작 |
|---|---|
| `"SMOKE"` | 합성데이터로 전 출력물 예행연습. 네트워크·키 불필요 |
| `"FULL"` | 계약검정 → 스모크 → 리허설 → 카나리 → 실데이터 수집 → 전체 (기본) |
| `"CACHED"` | 신규 수집 없이 구글드라이브 캐시만으로 재현 |

### 키는 어디서 받나 (전부 무료)

| 키 | 발급처 | 없으면 |
|---|---|---|
| `DART_API_KEY` | https://opendart.fss.or.kr → 인증키 신청 (즉시) | 시가총액이 근사로 낮아짐. 그 비중이 감사표에 표시됨 |
| `KRX_MARKETPLACE_ID/PW` | https://data.krx.co.kr → 회원가입 | **문제 없음.** 기본 비활성(§2) |
| `HANKYUNG_ID/PW` | https://consensus.hankyung.com → 회원가입 | 한경 소스 자동 스킵. 전략은 정상 동작 |

**DART 호출량은 상한을 미리 정하지 않습니다.** 남은 양을 실시간으로 추적해 그만큼 쓰고,
서버가 `020`(요청 제한 초과)을 줄 때 비로소 멈춥니다. `DART_API_KEYS` 에 키를 여러 개
넣으면 가용량이 선형으로 늘어나고(키당 20,000), 한 키가 020 을 받으면 그 키만 소진 처리하고
다음 키로 자동 전환합니다. 사용량은 (키해시, 날짜)로 드라이브에 기록돼 재실행 시 잔량을 바로 압니다.

---

## 2. KRX 없이 성립하는 PIT 유니버스 ★

KRX(`data.krx.co.kr` / pykrx / `kind.krx.co.kr`)는 **기본 비활성**(`NCQ_USE_KRX = False`)이다.
차단 이력이 있고, 무엇보다 **KRX 에 의존하지 않아도 결과가 성립해야** 하기 때문이다.

| 목적 | KRX-free 경로 | 성질 |
|---|---|---|
| 상장·폐지 명단 | FDR GitHub 정적 CSV (1956~, 상장일·폐지일·폐지사유·상장주식수) | KRX 서버 무관 |
| 상장·폐지 구간 | 일봉의 첫/마지막 거래일로 데이터에서 직접 복원 | 명단 결손의 최후 방어선 |
| PIT 상장주식수 | DART 주식총수현황, `knowledge_date` = **접수일자** | 증자·분할·감자 반영 |
| 시가총액 | PIT 상장주식수 × 월말 수정종가 (`merge_asof` backward) | 진짜 PIT |
| 가격 | 네이버 차트 → FDR → yfinance | 폴백 체인 |

KRX 를 켜면 월말 스냅샷으로 위를 **검증·보강**만 한다(합집합, 절대 교집합 아님).
어느 경로가 실제로 쓰였는지는 매 실행 「시가총액 소스 감사」·「데이터 소스 가용성 감사」에 나온다.

---

## 2-1. 가격 수집 — 헛수고를 먼저 걷어낸다 ★

실측에서 `P0.PX` 한 단계가 **60분**을 태웠습니다. 원인은 넷이고, 전부 구조적입니다.

| 원인 | 증상 | 조치 |
|---|---|---|
| 대상 미축소 | 종목마스터 5,398개를 그대로 조회. 1956~2015 폐지분·우선주·외국주 포함 | `price_targets()` — 백테스트 구간과 **상장구간이 겹치는 종목만**. 상장일·폐지일을 모르면 남긴다(생존자편향 방어) |
| yfinance 2배 요청 | 종목당 `.KS`+`.KQ` 2회 → 1만 회 초과 → `YFRateLimitError` → DNS 실패 | 종목 마스터의 시장으로 **1회만** |
| 실패 누적 무시 | 죽은 소스를 끝까지 두드림 | 소스별 **서킷브레이커**(연속 실패 임계 초과 시 이번 실행 동안 차단, 성공하면 리셋) |
| 체인 순서 | `pykrx` 선두 = 종목당 KRX 호출, QPS 2.0 → 그것만 45분 | `naver → fdr → pykrx → yfinance`. 네이버 차트는 10년치를 1회로 주고 전용 QPS 버킷(6.0)을 씀 |

추가로 종목별 요청 구간을 상장~폐지로 자르고, 1,000종목 청크마다 결과를 원장에 남겨
중간에 끊겨도 다음 실행이 **정확히 그 지점부터** 이어받습니다.

---

## 3. 실행 흐름

```
[0] 드라이브 캐시 연결 → 소스 배선 → 렉시콘·사전등록 동결(SHA)
    → 계약검정 N1~N11 → 합성 스모크 → 실경로 리허설 → 네트워크 카나리 C1~C7
[P0] PIT 유니버스   시총 하위 N + ADV 필터, 생존자편향 4중 방어
[P1] 리포트 인덱스  네이버·한경(+IR협의회) 최신→과거 역순, 월 청크, _done.jsonl 재개
     → 다중소스 병합 → 애널리스트 원장 → 커버리지 완결성 진단 → 유효 시작월 확정
[P2] 신규 커버리지  H1 de novo / H2 broker-new, PIT 브로커 합병, 스폰서 3분류
[P3] 본문          이벤트로 판정된 건만 PDF 수집 → TITLE/HEADLINE/BODY 분할
[P4] 스코어링      동결 렉시콘, 부정어미 무효화, 길이 정규화
[P5] 신호          같은 달 이벤트 풀 내 z-score → 상위 tercile (하위 tercile = Placebo)
[P6] 백테스트      12개월 오버랩 코호트 → 사전등록 P1~P4(BH-FDR) → 강건성 → 민감도 9조합
[R]  리포트        진단 9종 · 해석표 · 흐름지도 · report.html · run_manifest.json
```

---

## 4. 편향 제거

| 편향 | 대응 |
|---|---|
| **미래누수** | 이벤트 판정·신호에 쓰는 모든 정보는 `pub_date ≤ 월말`. 진입은 **익영업일 시가**. PIT 주식수는 결산일이 아니라 **접수일자** 기준. 완결성 진단의 '직후 12개월' 기준선은 진단 전용이며 신호에 쓰이지 않는다 |
| **생존자편향** | 폐지 종목 포함. 4중 방어(폐지원장 ∪ 가격구간 실측 ∪ DART ∪ KRX스냅샷). 근거가 없는 종목도 **버리지 않는다** — 버리는 순간 그게 편향이다. 폐지 시 -50% 후 현금화 |
| **선택편향** | 제외된 종목을 삭제하지 않고 `excl` 사유와 함께 보존. 3단 퍼널로 어느 게이트가 표본을 죽였는지 매 실행 출력 |
| **아카이브 결손** | 직후 12M 중앙값의 40% 미만 → 결손 태깅, 3개월+ 연속이면 그 이전을 배제. 유효 윈도우 5년 미만이면 **백테스트 전에 중단** |
| **스폰서 오염** | IR협의회는 자발적 커버리지가 아니므로 `is_sponsored` 태깅 + 3분류. P3(ORGANIC_ONLY 유지)가 헤드라인보다 우선한다 |
| **연구자 편향** | 렉시콘·사전등록을 **수익률 확인 전에** 동결하고 SHA256 을 매니페스트에 기록. 사후 수정 시 전 결과 무효 |
| **과적합** | 민감도 9조합만(81조합 금지), DSR 시행횟수에 9를 정직하게 반영, PBO/Holm 병기 |

---

## 5. 구글드라이브 캐시 — 절대 1원칙

**기존 캐시·인덱스를 훼손하지 않는다.** 약속이 아니라 **구조**로 보장한다:

1. 인덱스의 진실은 **append-only JSONL 저널**이다. 기존 줄을 다시 쓰지 않는다.
2. `index.parquet` 은 저널의 파생물이며 재생성 전 **항상 백업**. 백업 실패 시 컴팩션 자체를 건너뛴다.
3. 컬럼은 **합집합으로만** 확장한다.
4. blob 은 **내용해시 경로**라 덮어쓰기가 발생하지 않는다.
5. 이미 있던 파일은 **이동·개명 없이 경로만 등록**(adopt-by-reference).
6. **삭제 API 가 존재하지 않는다.** 손상 파일조차 `.corrupt` 로 격리만 한다.

```
{GDRIVE_ROOT}/
├── _shared/       ← 공용: 다른 전략(TCD v2 등)과 그대로 공유
│   ├── index/{index.jsonl(원천), index.parquet(파생), _backup/}
│   ├── blob/research/report_pdf/<해시앞2>/<해시>.pdf
│   └── table/{krx_ohlcv_daily, krx_marketcap_monthly, dart_shares_history,
│              research_report_master, report_index_{source}_{yyyy},
│              report_text_{티커앞2}, analyst_master, report_analyst_link, ...}
└── arc_ncq_v1/    ← 전용: 이 전략 고유
    ├── config/{lexicon_v1.json, prereg_v1.json}
    ├── index/ncq_p1_{source}_done.jsonl
    └── table/{universe_, signal_panel_, event_log_, backtest_returns_, cohorts_}
        outputs/{report.html, coverage_diagnostics.html, run_manifest.json, ...}
```

`GDRIVE_ROOT` 를 비워두면 환경에 맞게 자동 탐색한다(Colab 마운트 / Windows `G:\내 드라이브` /
`~/Google Drive/내 드라이브` …). 못 찾으면 로컬로 폴백하고 그 사실을 경고한다.

---

## 6. 자가검정 — 실데이터를 건드리기 전에 세 번 막는다

세 검정은 **서로 다른 것**을 본다. 하나로 대체할 수 없다.

| 검정 | 무엇을 증명하나 | 현재 |
|---|---|---|
| **계약 N1~N11** | 협상 불가 규칙(PIT·생존자편향·무기억성·동결·z 가드·코호트 회계·체결 앵커·상폐 -50%·캐시 무해성·결정성·누수 민감도) | 11/11 통과 |
| **실경로 리허설** | 네트워크만 가짜로 두고 **수집·정제 함수를 실물 실행**. 빈/깨진/컬럼누락 응답 내성 포함 | 48/48 통과 |
| **합성 스모크** | 유니버스→이벤트→텍스트→신호→백테스트→성과→강건성→리포트 **계산경로 전체** | 통과 (10초) |
| **네트워크 카나리 C1~C7** | 실제 사이트 접근성. 특히 **C3(36개월 전 아카이브)** — 실패 시 10년 백테스트 불가 | 실행 시 판정 |

---

## 7. 에러 국소화

모든 연산은 `PIPE.stage(...)` 안에서만 돈다. 실패하면 자동 출력되는 것:

- **실패 지점**: 스테이지 ID · 계층 · 경과시간
- **직전 입출력 스냅샷**: 무엇이 몇 행 들어오고 나갔는지, PIT 컬럼 유무까지
- **한글 진단 힌트**: 예외 패턴 → 원인·조치
- **트레이스백 마지막 12줄**

실행 끝에는 스테이지 요약 · **데이터 흐름 원장(I/O LEDGER)** · PIT 저장소 상태 ·
HTTP 수집 감사 · 소스 가용성 · Phase 예산 · 데이터 흐름 지도가 출력된다.

**실패했을 때 보는 순서**: ① 스테이지 요약 → ② I/O 원장(0행이면 수집 실패) →
③ 커버리지 완결성 진단 → ④ 3단 퍼널(어느 관문이 표본을 죽였나) → ⑤ 헤드라인(열화 적용 여부).

---

## 8. 개발

```bash
python3 tools/build_ncq.py     # build/ 코어 + build_ncq/ 조각 → strategies/*.py 조립
python3 tools/check_ncq.py     # 조립본 정적 점검(미정의 이름·중복 정의·계약 함수 부재)
```

`build_ncq/_CONTRACT.md` 가 모듈 간 인터페이스 계약이다. 새 모듈을 추가할 때 먼저 읽을 것.

---

## 9. 알려진 한계 (숨기지 않는다)

1. **네이버 리서치의 실제 과거 아카이브 깊이는 실측 전까지 알 수 없다.** C3 카나리와 완결성
   진단이 결정한다. 3년만 나오면 10년 백테스트는 불가능하며, 그 사실이 리포트 최상단에 뜬다.
2. **월평균 이벤트 수는 Phase 1 완료 후에야 안다.** 5건 미만이면 횡단면 z 가 불안정해지고
   검정력이 부족하다. 그 경우 경고가 최상단에 표시된다.
3. **DART 키가 없으면** 시가총액이 '현재 주식수 × 과거 종가' 근사로 낮아진다. 유상증자가 잦은
   소형주 구간이라 '하위 N' 경계가 흔들리며, 근사 비중이 감사표에 그대로 나온다.
4. **관리종목·투자주의환기종목 지정 이력은 PIT 확보가 어렵다.** 거래대금 기반 대용 지표를 쓰며
   그 사실을 명시한다.
5. **렉시콘은 연구자 편향을 담고 있다.** 동결·버전관리로 통제하되, 수익률 확인 후 수정하면
   전 결과가 무효다.
6. **이 전략은 검증 전까지 알파로 취급하지 말 것.** 기존 검증 전략을 코어로 두고, 채택 조건을
   모두 통과한 뒤에만 위성 배분 후보로 고려한다.

---

*본 문서와 코드는 전략 설계·검증용이며 투자자문이 아니다.*
