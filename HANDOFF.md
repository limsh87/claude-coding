# TCD v2 인수인계 — 다음 세션이 이어받을 것

작성 2026-08-11 · 브랜치 `claude/strategy-backtest-robustness-ge7x1n` · PR #14 (draft)
최신 커밋 `ea76560` · 본체 `tcd_v2_zerobase.py` 13,061행

---

## 0. 지금 상태 한 줄

외부 감사가 지목한 **치명 4건 + 부수 3건을 반영해 신호의 부호와 방향이 바뀌었다.**
따라서 **이 커밋 이전의 모든 성과 수치는 전략에 대한 정보가 아니다.** 재실행이 선행되어야 한다.

---

## 1. 이번에 반영된 것 (커밋 `ea76560`)

| # | 항목 | 무엇이 문제였나 | 무엇을 했나 |
|---|---|---|---|
| ① | **TP 부호 분면** | 실수의 곱은 음×음=양. (개선↓·대가↑) 즉 **축소·부실 기업**이 (개선↑·대가↓)와 똑같이 큰 양수를 받았다. 상위 5%만 뽑으므로 그 꼬리가 전부다 | `TP_SIGN_GATE="gain"` — 개선축이 양수일 때만 TP 성립. 실측 (−,−) 오염 0.00% |
| ② | **D축 진입 게이트** | `D_state` 를 계산해 놓고 리포트에만 썼다. 이익 붕괴 > 주가 붕괴면 "멀티플 축소"로 읽혀 최고점 | `D_ENTRY_GATE=True` — `d1` 은 `ΔlogE>0` 일 때만 성립 |
| ③ | **ΔlogM 주식수** | `ΔlogM = ΔlogP − ΔlogE` 로 `Δlog(주식수)` 누락 → **자사주 소각 기업**(PACK-C 의 표적)을 체계적 오평가 | `+ Δlog(shares)`. 주식수는 이미 수집 중(추가 호출 0) |
| ④ | **θ_X 차원** | 분자 월간 수출 / 분모 TTM 매출 → 수출 100% 기업도 θ≈0.083 → V4 가 PACK-X **전량 사살** 후 "데이터 부재"로 오진 | `THETA_X_ANNUALIZE=True` |
| ⑤ | **θ 결측 0 심기** | `fillna(0.0)` 의 0 은 결측이 아니라 **값**이라 하한선 판정에 참여 → 빈 축으로 하한선 통과 | `THETA_KEEP_NA=True` |
| ⑥ | **진입 후보** | Signal 0 이 동점 → tie-break 이 code 오름차순 → 후보 마르면 **코드 번호순 5종목** 매수 | `ENTRY_MIN_SIGNAL=True`, 모자라면 그만큼만(현금) |
| ⑦ | **수집 시간 제한** | 사용자 지시로 해제 | `HARVEST_TIME_BUDGET_H=0`, 시간 몫 해제, 파일 마감선 7200s. **정체 방지는 유지** |

**중요한 구분**: ①은 우리 구현의 버그가 아니라 **원전(`build/03_util.py:485 tp_product`)에도 있는
계약 자체의 결함**이다. 그래서 몰래 고치지 않고 `TP_SIGN_GATE` 로 **명시 선언**하고 끌 수 있게 뒀다.
게이트를 개선축에만 건 근거: 이 저장소가 이미 `TP_P2` 한 곳에서 그렇게 하고 주석에 같은 논리를
적어 뒀다(나머지 15곳에 안 걸었을 뿐). 양쪽에 걸면 결측 75%, 개선축만이면 50%.

---

## 2. 아직 안 한 것 — 우선순위 순

감사 보고서 항목 중 **미반영**. 각각 근거와 착수점을 적는다.

### 2-1. 【D】 셀 내 랭크 → 월 전체 선정 (얇은 셀 편중) — **최우선**
- 감사 주장: 얇은 셀(<20종목)이 유니버스 49.4% ↔ 포트폴리오 93.8%, 실현 알파 1.78→1.17
- **미검증**: 이 수치를 내가 재현하지 못했다. 반영 전 반드시 재현할 것
- 착수점: `parts/p13_score.py` `score_by_axes()` 의 `E = rx(P, E_raw)`
- 후보 패치: `shrink_k = sqrt(n_cell/(n_cell+30))`, `E_adj = 0.5 + (E-0.5)*shrink_k`
- 주의: C5(winsorize→z→rank)·C11(셀 정의)을 건드리므로 계약 영향 검토 필요

### 2-2. 【E-3】 블록 평균이 TP 를 증발시킴 → R2 판정 오염
- TP 블록 전무 + 추세만 있으면 `E_AXB = z(b1)` 인데 "증거층 B축"으로 표기됨
- → R2 가 TP팔과 나이브팔로 **같은 것을 비교**할 수 있다
- 착수점: `tp_share` 컬럼 신설(`parts/p12_features.py`), R2 에서 낮은 행 제외 또는 명시

### 2-3. 【G-2】 R2 두 팔 정렬
- 현재 두 팔이 ①TP곱 ②θ가중 ③추세블록 ④축개수 **4가지가 동시에 다름** → 귀속 불가
- 나이브팔도 같은 θ·추세블록·축개수로 두고 **TP 곱만 단독 교체**해야 함
- ⚠ **지금까지의 R2 실패 판정은 재사용 금지** — TP 가 뒤집힌 상태의 판정이었다

### 2-4. 【G-1】 R1c 부호 정합성 검정 신설
- R1 은 미래수익 주입 오라클이라 **배관만** 검정한다. 신호가 뒤집혀도 통과한다
- 리허설에는 이미 넣었으나(`smoke/rehearse_v2.py` TP 부호 분면) **본체 R 시리즈에는 없다**

### 2-5. 【B-2】 U 축 개수 불일치
- 적자기업은 `dlog_E=NaN` → d1 결측 → 흑자 2축 vs 적자 1축이 **한 줄로 랭크**됨
- `U.fillna(0)` 도 남아 있음(연속 점수인 척하는 하드 배제)
- 착수점: `parts/p12_features.py` U 산출부. `U_n_axes` 존재 여부부터 확인

### 2-6. 【E-5】 `value_added` payroll 결측 전파
- `payroll.fillna(0)` 이라 DART 한도 소진 시 부가가치가 **EBITDA 근사로 변질**
- 이 값은 `TP_C2`·`TP_N4` **두 TP 의 우변**이다

### 2-7. 【E-2】 zx 폴백 사다리 (폴백률 실측 37.7%)
- `cell`/`cell_up`/`cell_all` z 를 한 컬럼에 섞음 → "산업 상대"와 "시장 상대"의 혼합물
- 착수점: `z_level` 컬럼으로 레벨 기록 후 레벨별 재표준화

### 2-8. 【F-2】 `fwd_ret` 결측 0% 처리 · 【F-3】 산업분류 비PIT · 【F-5】 `roll_ols` win=36
### 2-9. 【F-4】 `USDKRW_CONST=1300` 고정 → 월별 실환율 (θ_X 에 ±25% 오차)

### 2-10. 성능 최적화 — **전혀 착수 못 함**
사용자 지시: "벡터화, 램저하 안 일으키고 차단 안 당할 수준의 멀티프로세싱, 대용량 벌크화".
워크플로를 띄웠으나 3시간 후 결과 0건으로 중단됐다. **처음부터 다시 해야 한다.**
조사 대상은 `workflows/scripts/tcd-perf-bottlenecks-wf_8682ba38-5e6.js` 에 6개 구간으로 남아 있다:
네트워크/벡터화/RAM/멀티프로세싱/중복호출/엔진. **에이전트에 위임하지 말고 직접 하라** —
고효율 에이전트 다수를 3시간 돌려 아무것도 못 얻었다.

---

## 3. 미해결 인프라 (이전 세션부터)

| 항목 | 상태 |
|---|---|
| **PACK-N 10년치** | 파일 아카이브 경로 구현 완료(`harvest_nps_file`, serviceKey 0콜). 씨앗 27개월(2024-01~2026-03). **2016년대 uddi 생존 여부 미확인** — 이 컨테이너가 data.go.kr 차단이라 실행 시 3콜 프로브로만 확인 가능 |
| **회전율 96% · 비용 91% 잠식** | 미착수. `weigh_positions` 실효 8.7종목(보고 17.4는 허구), `Sharpe=cagr/vol`(표준의 1/5.8), `IMPACT_K=0.10` 과대 + 스프레드 항 없음 |
| **U축 붕괴** | d2·d3·d4 전멸 → P.ROBUST '판정 불가'. 2-5 와 함께 봐야 함 |
| **구글드라이브 미탐지** | Windows 에서 LOCAL_ONLY 로 떨어짐 |
| **KRX 로그인 / pykrx** | 3경로 전멸, 임포트 실패. 원인 노출까지만 해 뒀음 |

---

## 4. 작업 방법 (반드시 이대로)

```bash
# 편집은 ★파트 파일에만. 조립본을 직접 고치면 다음 조립에서 날아간다.
cd <scratchpad>/parts/          # p00_config.py … p17_main.py (19개)
vi p12_features.py
bash ../assemble.sh             # → /home/user/claude-coding/tcd_v2_zerobase.py + py_compile

# 검증 3종은 ★전부 통과해야 커밋한다
python3 ../smoke/rehearse_v2.py   # 171개 불변식 (산식 드리프트 잠금 포함)
python3 ../smoke/wiring_v2.py     # main() 21스테이지 배선 (네트워크 0회)
python3 -c "s=open('/home/user/claude-coding/tcd_v2_zerobase.py').read().replace('RUN_MODE = \"FULL\"','RUN_MODE = \"SMOKE\"',1); exec(compile(s,'x','exec'),{'__name__':'__main__'})"
```

**리허설이 지키는 것**: 원전(`build/p_*.py`)에서 TP 인자·부호·개수를 직접 읽어 대조한다.
`tp_pair` **내부**를 바꾸는 것은 통과하지만 **인자를 바꾸면 실패**한다 — 의도된 설계다.

---

## 5. 사용자의 고정 요구사항 (매 메시지 반복됨 — 어기면 반려)

1. **절대 1원칙**: 신규 수집분은 반드시 구글드라이브 공용(`_shared`)/전용(`tcd_v2`) 인덱스에
   저장해 재호출 가능하게. **기존 캐시를 어떤 경우에도 훼손하지 않음.** 배치마다 즉시 적재.
2. 로컬(D드라이브)과 드라이브 **양쪽 다** 캐시 탐색
3. 키값 상단 고정 + 어디서 얻는지 직관적 설명
4. **차단 안 당할 정도의** 속도 최적화 (국내 서버는 과속 시 HTTP 200 에 봇차단 페이지를 실어 보낸다)
5. **"쓸데없이 가격조회 반복하지 마"**
6. **"키호출량을 처음부터 고정하지 말고 남은 호출량을 실시간 체크"**
7. 시총 하위 1000 스몰캡 비교전략을 본전략과 함께
8. **관세(PACK-X)·국민연금(PACK-N)이 "제일 중요"** — 자동수집 필수
9. **"정의드리프트 변경 산식훼손 절대 없게"** — 단 2026-08-11 감사 반영은 명시 승인됨.
   계약을 바꿀 때는 **반드시 명시 선언**(상수 이름 + 주석 + 리허설 불변식)
10. 작업순서: 미시(로그·코드 최소단위 분해) + 거시(전체 흐름) → **핀포인트/부분개편/전면개편
    방향을 먼저 정한 뒤** 수정
11. 완료 시 다운로드 링크/미리보기 링크 제공

---

## 6. 다음 세션 첫 3수 (권장)

1. `RUN_MODE="FULL"` 로 **재실행**. ①~⑦ 반영 후 신호 방향이 바뀌었으므로 기준선을 새로 잡는다.
   시간 제한이 풀렸으니 완주까지 오래 걸린다 — 원장이 이어받으므로 나눠 돌려도 된다.
2. 2-1(셀 shrinkage)을 **재현부터** 하고, 재현되면 반영.
3. 2-10(성능)을 직접 착수. 가장 큰 건 중복 호출 제거일 가능성이 높다(사용자 반복 지적).
