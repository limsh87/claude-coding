# QVF-FUNNEL v1.0 — 인수인계 문서

> 다음 대화창으로 넘기는 문서. **이 파일만 읽으면 이어서 작업할 수 있어야 한다.**
> 마지막 갱신: 2026-08-10 · 브랜치 `claude/qvf-funnel-v1-setup-s5lnux` · PR #16(draft)

---

## 0. 한 줄 요약

한국 소형주 3단 깔때기 전략의 10년 백테스트를 **자립 실행 파일 1개**로 구현했다.
계산 경로는 검증됐고, 실데이터 수집도 완주 직전까지 갔다. **남은 일은 명세 전수조사**다.

---

## 1. 사용자의 원 지시사항 (계약문서 — 축약 없이)

원문 명세서는 저장소에 없다. 대화로만 전달됐고, 조항은 코드 주석에 `§3.1` 형태로 박혀 있다.
아래는 사용자가 반복적으로 강조한 요구사항 전문이다.

### 1-1. 전략 정의

```
U-1000  PIT 시총 하위 1000 (KOSPI+KOSDAQ, 폐지종목 포함)
  └─[1차] V / Q / F 스코어 — 3변형 V · VQ · VQF ─────────→ U-200
        └─[2차] ΔNONFIN + ΔTONE(결측허용) − 배제플래그 ──→ 60~80
              └─[3차 3-A] 규칙 체크리스트 ──────────────→ 20~40 종목
```

- 백테스트 2016-08 ~ 2026-07 (10년), 분기 리밸런싱 (3/1, 6/1, 9/1, 12/1)
- 신호는 직전 거래일 종가까지, 체결은 익 거래일 **시가**

### 1-2. 절대 1원칙 (사용자 원문)

> "어떤일이있어도구글드라이브에저장된 캐시의 전용인덱스, 공용인덱스를훼손하지마라"
>
> "반드시 구글드라이브에 재수집된 데이터 및 보고서를 공용인덱스/전용인덱스로 저장및
> 재호출가능해야 한다. 절대1원칙이다!!"
>
> "캐시탐색은 로컬d드라이브와 구글드라이브 양쪽에서 하되 신규수집되는 모든 데이터 및
> 자료는 구글드라이브에 저장하라"

- 로컬 D: 가 없는 환경(Colab)에서는 드라이브만 탐색 — 그것이 정상 경로다
- **이전 버전·다른 전략이 남긴 캐시도 재활용해야 한다** (스키마가 달라도)

### 1-3. DART 호출량 (사용자 원문)

> "키호출량을 처음부터 19000으로 정하지 말고 남은 호출량을 실시간으로 체크해서
> 그만큼 쓰게 하라"
>
> "애초에 4시간짜리 백테스트인데 2~4만회 호출량이 왜 부족하냐고!!
> 니가 설계를 비효율적으로 잘못한거아냐!!!!"

### 1-4. 그 밖의 명시적 요구

| 요구 | 상태 |
|---|---|
| 원셀실행형 — Colab / JupyterLab 양쪽 | ✔ 빌더가 AST 로 강제 |
| 키값 상단 고정 + 어디서 얻는지 직관적 설명 | ✔ 헤더 ①~⑤ |
| 차단 안 당할 속도최적화 (멀티프로세싱·벌크·벡터화) | 부분 — §5 참조 |
| FDR·pykrx·네이버·yfinance·KIND 다중소스 PIT 유니버스 | ✔ 시총까지 다중화 |
| 한경컨센서스·네이버리서치 · 애널리스트↔리포트 연결 감사 | ✔ |
| 생존자편향·미래참조 등 편향 선제 제거 | ✔ 계약 Q1~Q14 |
| **비교용 시총하위 1000 스몰캡 벤치마크 동시 백테스트** | ✔ X0 |
| 멀티에이전트 적극 사용 | ✔ 5회 — 결정적 결함 다수 발견 |
| 파이썬 파일로 미리보기 없이 다운로드 링크 | ✔ |

---

## 2. 지금 구조

```
build/                    조각 파일 (TCD v2 와 공용 코어 공유)
  00~05, 10~14, 20        공용 코어 — 다른 전략과 같은 파일
  q00_header.py           QVF 설정 헤더 (키·상수)
  q06_vault_ext.py        다중루트 캐시 + DartQuota
  q21~q26, q40~q60        QVF 전략 계층
  q70_contracts.py        계약 Q1~Q14
  q80_selftest.py         스모크 + 리허설
  q90_main.py             오케스트레이터
tools/build_qvf.py        조립기 (도달성 분석 · 원셀 적합성 AST 검사)
strategies/qvf_funnel_v1.py   ← 산출물. 이것 하나만 실행하면 된다
```

**빌드**: `python3 tools/build_qvf.py`
**검증**: 아래 3개가 전부 통과해야 한다

```bash
# 1) 파일 실행 (RUN_MODE=SMOKE 로 치환해서)
# 2) 원셀 실행 시뮬레이션 — Colab 처럼 파일 없이 exec
# 3) 조립기 자체 검사 (문법·중복정의·정의전참조·inspect 금지)
```

원셀 시뮬레이터는 `compile(src, "<ipython-input-1-abcdef>", "exec")` + `exec(code, {"__name__":"__main__"})`
로 `__file__` 없이 돌린다. Colab 실행을 죽였던 버그를 재현하는 유일한 방법이다.

---

## 3. 실행 순서 (파이프라인)

```
L0.VAULT     캐시 연결 (드라이브 쓰기 + 로컬 미러 읽기)
L0.CONTRACT  계약 Q1~Q14 — 실패하면 수집을 시작하지 않는다
L0.SMOKE     합성데이터 엔드투엔드
L0.REHEARSAL 네트워크만 가짜로 두고 수집함수 실물 실행
L1.UNI       종목 마스터 (FDR + KIND + 폐지목록)
L1.CAL       거래일 격자 · 분기 캘린더        ← 지수 1호출
L1.CAP       PIT 시가총액 스냅샷              ← 후보 확정의 근거
L1.PX        일봉 (U-1000 후보만)
L1.DART      Tier-1 주요계정(배치) + Tier-2 전체재무제표
L1.DIS       공시목록 스윕
L1.FLOW      외국인·기관 순매수
L1.RESEARCH  리포트 목록 + 원장 (상세·PDF 없음)
L1.PANEL1    U-1000 → V/Q/F 축 → 1차 필터 → U-200
L1.FACTS     DART 하드팩트 (U-200 합집합)     ← need_codes 확정
L1.TONE      상세 보강 + PDF (U-200) → TONE 분류기
L2.PANEL2    2차 필터 · 3-A                   ← 무력화 감지
L3.MAIN/ABL  주 실험 3 + 어블레이션 X0~X4
L5.FDR/ROBUST BH-FDR · 강건성
L6.*         Phase0 · 판정 · 산출물
```

**설계 원칙 (반복해서 어겼다가 고친 것)**

1. **수집은 깔때기 뒤에 놓는다.** 무엇을 받을지는 무엇을 쓸지가 정한다.
   L1.CAP → 후보 → L1.PX 순서, L1.FACTS → need_codes → L1.TONE 순서가 그 결과다.
2. **실패는 작업을 좁혀야지 넓히면 안 된다.** 소스가 죽었다고 전 종목을 받으면 안 된다.
3. **못 물어본 것을 "없다"고 기록하지 않는다.** 예산 소진과 자료 없음은 다르다.
4. **능력 표가 거짓말하면 안 된다.** 설치 여부가 아니라 실제 import 결과를 적는다.

---

## 4. 지금까지 고친 결함 (재발 방지용 — 같은 부류를 다시 만들지 말 것)

### 4-1. 조용한 열화 (silent degradation)

| 결함 | 증상 | 원인 |
|---|---|---|
| pykrx import 실패 삼킴 | 표엔 "설치됨", 실제론 None → 시총 0 → 전 종목 5,398 수집 | `except: x = None` |
| KRXGate "실패 0건" | 전 호출 실패인데 0건 보고 | pykrx 가 예외를 삼키고 `print()` 로 뱉음 |
| DART 센티넬 오염 | 예산 소진분을 "데이터 없음"으로 **공용 캐시에 영구 기록** | 실패 종류를 전부 None 으로 뭉갬 |
| 일봉 캐시 소실 | 드라이브 플레이스홀더를 손상으로 보고 `.corrupt` 개명 → 전량 재수집 + 폐지종목 영구 소실 | 보호가 인덱스에만 걸림 |
| 2차 필터 무력화 | DART 비면 Score2 전 행 동일 → 선정이 1차 점수로 떨어지는데 이름은 그대로 | 결측을 0(중립)으로 채움 |
| **TONE 축 무의미** | CV 0.77 인데 tone↔진짜부호 상관 **−0.008** | 문서로 학습, 문장으로 채점 |

### 4-2. 반복 수집

| 결함 | 규모 | 원인 |
|---|---|---|
| 무한 재수집 루프 | 1,974종목 매 실행 | 늦게 상장한 종목의 캐시 최소일을 결손으로 오판 |
| 재수집 3,492종목 | 캐시 3,497 중 3,492 | 판정이 시도 원장에만 의존 (원장은 구버전이라 비어 있었음) |
| 폐지종목 매 실행 재수집 | 500~900종목 | forward 분기에 폐지일 판정 없음 |
| 리서치 10년 재스윕 | 약 3시간 | 수집기가 캐시를 아예 안 읽음 |
| PDF 19.8만건 | 34시간 | 소비처(U-200)와 무관하게 전 코퍼스 |
| 네이버 상세 5,434건 | 36분 | U-200 아닌 U-1000 후보로 좁힘 (잘못된 단계) |

### 4-3. 성능

| 결함 | 규모 | 원인 |
|---|---|---|
| 일봉 55분 | — | `limiter("krx")` 2.0qps **전역** 버킷 + FDR 이 같은 버킷 공유 |
| pykrx CD011 폭풍 | 종목당 요청 4~8배 | 12스레드가 각자 재로그인 → `skipDup` 이 앞 세션 종료 |

**⚠ 위 두 건은 처방을 한 번 틀렸다가 되돌렸다(`9588e6d`).** 첫 처방은 "FDR 전용 버킷 분리 +
`_px_pykrx` 를 `KRXG.call` 경유"였는데 둘 다 틀렸다.

- `pykrx.get_market_ohlcv(adjusted=True)` 는 **KRX 를 두드리지 않는다** — 내부적으로
  `pykrx.website.naver` → `fchart.stock.naver.com`. KRX 락에 줄만 서서 병렬성을 잃었다.
  게이트는 애초에 옳은 방어선이 아니다: **자기를 통과하는 호출만** 직렬화하므로 다른
  스레드풀의 직접 호출은 그대로 샌다. 로그인 폭풍은 `01_bootstrap` 이 pykrx
  `auth`/`webio` **경계에 락**을 걸어 막는다(두 바인딩 모두 교체 — `webio` 가 함수를
  이름으로 import 했으므로 `auth` 만 패치하면 효과가 없다). 호출 지점을 하나도 놓치지
  않는 유일한 지점이다.
- FDR(6자리)의 기본 리더도 `NaverDailyReader` → **같은 호스트**. 버킷을 나누면 그 호스트가
  두 상한의 **합**을 맞는다 — 계량표는 지키면서 실제 서버만 초과당하는, 원래 지적했던
  결함의 거울상이다. 한 호스트 한 버킷(`naver` 2.5qps)으로 통일, `"fdr"` 버킷 삭제.

**교훈**: 병목이 사라졌다는 사실은 처방이 옳았다는 증거가 아니다. "이 경로가 실제로 어느
호스트를 치는가"를 확인하지 않고 계량 버킷을 나눈 것이 두 오류의 공통 원인이다.
| **stdout 사망** | 실행이 멈춘 것처럼 보임 | `capture_noise` 가 `sys.stdout` 을 **전역 교체** (스레드가 서로의 버퍼를 원본으로 저장) |
| build_momentum | 800만행 80패스 | 리밸 시점마다 merge_asof |
| DART 22만 호출 | 8.7일 | (회사 × 전연도 × 4분기) 데카르트 곱 |

### 4-4. 계약층 자체의 결함 (검정력 0)

다중 에이전트 감사로 발견. **"통과했다"가 "보장된다"를 뜻하지 않았다.**

| 계약 | 무엇이 없었나 |
|---|---|
| Q6 | VQ 가중치를 **아예 검사하지 않음**. VQF 는 "합이 1"만 봄 |
| Q8 | **실측 검정력 0** — 동점이 커트라인을 안 가로질러 tie-break 를 없애도 통과 |
| Q10 | 제목은 "비용 차감 후 ≤ 차감 전"인데 **백테스트를 돌리지도 비교하지도 않음** |
| Q11 | 단방향이라 z 가 **전부 NaN 이어도 통과** |
| Q12 | **실측이 성공할수록 계약이 깨지는 구조** (실효값이 헤더 상수와 같아야 통과) |
| Q1 | 빈 프레임 우회 경로 미검사 |
| Q7 | 이름 5개 블랙리스트 — `evict`/`prune` 으로 바꾸면 통과 |
| pinned_src | 셀에서 함수를 고치면 **돌지 않는 코드를 검정하고 ✔** |

전부 행위 검정으로 교체했다. **새 계약을 쓸 때는 "이 검정이 실패하는 입력을 만들 수 있는가"를
먼저 확인할 것.**

### 4-5. 명세 전수조사가 잡은 것 (v1.1 · `L0.SPEC` 게이트)

계약 Q1~Q14 는 **원칙**(PIT·생존자편향·결정성)을 지킨다. 전수조사는 **숫자**를 지킨다 —
"§5.3 이 지정한 5개 지표가 정말 5개 다 산출되는가" 같은 것. 아래 5건은 계약·스모크·리허설을
전부 통과한 상태에서 나왔다.

| # | 결함 | 왜 안 잡혔나 · 실측 |
|---|---|---|
| 1 | **Q축 5개 지표 중 3개가 z 를 하나도 만들지 않았다** — 발생액·ROIC 3년 표준편차·주식수 3년 증가율 | `z_lower_is_better` 가 3상태 `valid` 의 `<NA>`(=분모 개념 없음)를 `fillna(False)` 로 뭉갠 뒤 그것을 **z 표본 조건으로도** 썼다 → 표본 0 → 전 행 NaN. 실측: 관측률 100% 합성패널에서 `raw 20 → z 0`. 스모크 로그의 "지표 5개 중 평균 **2.00**개 가용"이 흔적인데, 코드는 이것을 'DART 수집 미완'으로 **잘못 안내**했다(원인은 수집이 아니라 이 한 줄) |
| 2 | **§10.4 ②(깔때기 기여 미미)가 사실상 항상 발동** → 기본 실행이 매번 폐기 판정으로 끝난다 | `paired_diff_test` 만 `hac_tstat` 을 `t, _se = ...` 로 받았다(나머지 8개 호출부는 전부 `mu, t`). t 자리에 **평균**(≈0.001~0.05)이 들어가 `p ≈ 0.48` → `p ≥ 0.10` 항상 참. §9-C2 도 같은 이유로 영영 통과 못 한다. 스모크는 n<12 → nan → 폴백 경로라 **드러나지 않는다**(40분기 실행에서만 나타남) |
| 3 | **pandas 3.x 에서 L0.CONTRACT 에서 즉사** | `E.loc[resolve,"fwd_ret"] = ...` 가 float32 열에 float64 를 넣는다. pandas 2.x 는 조용히 캐스팅, 3.x 는 `TypeError`. 하필 생존자편향 방어 경로라 **계약 Q3 가 실패**하고 수집 전에 멈춘다 |
| 4 | **pandas 3.x 에서 §3.2 유동성 게이트 전멸** | `stack(dropna=True)` 가 3.x 에서 `ValueError`. 같은 저장소의 `p_x_customs.py` 는 이미 그 이유로 melt 를 쓰는데 여기만 남아 있었다. `merge_asof` 키의 `datetime64[us]` vs `[ns]` 불일치(3.x 기본 해상도 변경)도 같은 부류 — `as_ts`/`as_ts_series` 에서 ns 로 못박아 해결 |
| 5 | **X0 스몰캡 벤치마크 표가 통째로 nan** / `ADTV_MIN_KRW` **미정의 전역** | 전자는 `qperf_stats` 키가 `'CAGR'` 인데 `'cagr'` 로 조회(크래시하지 않고 nan 이 찍힌다) — §10.2 귀속의 비교 기준선이 읽을 수 없었다. 후자는 함수 **본문 안**이라 조립기의 '정의 전 참조' 검사(최상위만 본다)가 못 잡는다 → 그 분기 ADTV 가 전부 없으면 `NameError` |

| 6 | **`to_code6` 가 없는 티커를 만들어냈다** — 함수 자신의 약속을 깨고 있었다 | 문자가 섞인 코드에서 숫자만 뽑아 zfill: `00341A`(쌍용양회**4우B**) → `000341`(쌍용양회 **보통주**). **우선주의 폐지일이 보통주에 붙는다** → 상장 중인 종목이 유니버스에서 사라지고 보유분이 −100% 로 청산된다. FDR 폐지목록 실측 4,173행에서 원본≠매핑 **285건**, 그중 주권 **13건** |
| 7 | **"폐지목록 37% 탈락 = 생존자편향 잔존"은 오경보였다** | 증권 유형별로 분해: 탈락 1,821건 = 신주인수권증서 865 · 수익증권 783 · 신주인수권증권 160 · **주권 13**(전부 구형 우선주, §3.3 이 어차피 제외). U-1000 은 주권만 담으므로 **버리는 것이 옳다.** 비율만 경고하면 남지도 않은 편향을 과대보고한다 |

3·4 는 **사용자 환경(py3.14)에서 pandas 3 이면 L1.PANEL1 에 도달조차 못 한다.** 지금까지
L1.DART Tier-2 에서 멈춰 있었기 때문에 아직 노출되지 않았을 뿐이다.

전수조사는 §구조 검정으로 5번 같은 부류를 자동 검출한다(이미 정의된 코드객체에서 LOAD_GLOBAL 을
걷어 실행 네임스페이스·빌트인과 대조 — 소스 파일이 없는 원셀 실행에서도 돈다).
검정력 확인: 수정 전 소스에 돌리면 `{'ADTV_MIN_KRW': 'run_qbacktest'}` 를 정확히 집는다.

**v1.1 에서 전수조사는 실행 파일 안으로 들어갔다.** `L0.CONTRACT`(원칙) → `L0.SPEC`(숫자)
순서로 게이트가 돌고, 실패하면 수집을 시작하지 않는다(약 1.8초). 인수인계 계약이
"이 파일 하나만 실행하면 된다" 이므로 검정이 `tools/` 에만 있으면 Colab 사용자는 못 받는다.
`tools/audit_spec_qvf.py` 는 조립본의 `run_spec_audit()` 를 부르는 얇은 러너다.

검정층은 전역을 만지므로(`U1000_N` · `EXPERIMENTS` · 거래일 캘린더 …) 실행 전후로 스냅샷을
떠서 복원하고 **복원 여부 자체를 검정한다.** 검증층이 본 실행의 입력을 바꾸면 오염원이 된다.
새 검정을 추가하면서 전역을 만졌다면 `_SPEC_GUARDED` 에 그 이름을 반드시 넣을 것.

---

## 5. 지금 상태

### 검증된 것
- 계약 Q1~Q14 · 스모크 · 리허설 — **파일 실행 / 원셀 실행 양쪽 rc=0**
- **명세 전수조사 §3.1~§10.4 — 조항 24개 · 소검정 221개 전부 통과** (pandas 3.0.5 / py3.11)
  `python3 tools/audit_spec_qvf.py`
- 사용자 실환경(Windows·py3.14·JupyterLab)에서 L1.DART Tier-2 까지 진행 확인
- 캐시 재사용: 일봉 재수집 0건, 시총 폴백 네트워크 0회, Tier-1 663회로 2,897사

### 실측 수치 (사용자 환경)
| 항목 | 값 |
|---|---|
| 일봉 대상 | 5,398 → 2,652 (51% 절감) · 30초 |
| DART Tier-1 | 663회로 2,897사 (단건이면 66,300회) |
| DART Tier-2 | 144,032 → 19,154 (7.5일 → 1.0일) |
| DART 실측 한도 | **22,079건** (코드 고정값 아님) |
| 시총 폴백 | 88,898행 (2,716종목 × 40시점) · 신규 호출 0 |

### 미검증 / 알려진 문제
1. ~~명세 전수조사 미완~~ → **완료**(§4-5) · v1.1 에서 `L0.SPEC` 게이트로 내장. 다만 전수조사는
   **계산 경로**를 검정한 것이지 실데이터 성과를 검정한 것이 아니다. §4-5 의 1·2번은
   **선정 결과와 최종 판정을 바꾸므로 그 전에 나온 성과 수치는 전부 무효다** — FULL 재완주 필요.
2. ~~pykrx import 실패 (Windows cp949)~~ → **자동 우회 추가**. 윈도우에서 1차 import 가 깨지면
   재시도 동안만 `locale.getpreferredencoding` 을 UTF-8 로 보이게 해 복구를 시도하고, 복구
   여부를 `IMPORT_FAILURES` 표에 남긴다(전역은 되돌린다). 근본 해결은 여전히 `PYTHONUTF8=1`.
   **사용자 환경에서 실제로 복구되는지는 미검증** — 이 컨테이너는 리눅스라 재현할 수 없다.
3. **KRX 마켓플레이스 로그인** — 응답은 정상인데 인증 호출이 데이터를 안 줌 (bld 변경 추정).
   **미해결.** KRX 실계정·네트워크가 있어야 진단 가능하다.
4. ~~드라이브 미발견 시 "드라이브로 승격 복사" 오출력~~ → **수정**. 실제 목적지를 찍는다
   (`구글드라이브` / `지정 쓰기루트(...)` / `로컬 쓰기루트(...) — 드라이브가 아닙니다`).
5. ~~폐지목록 37% 탈락 = 생존자편향 잔존~~ → **오경보였다.** 유형별로 분해하니 탈락분에
   보통주가 0건이다(§4-5 7번). 대신 그 조사에서 **훨씬 나쁜 티커 날조**가 나왔다(§4-5 6번).
6. ~~Tier-2 "예상 일수"가 `19154.0일`~~ → **수정**. 분모를 '오늘 잔여'가 아니라 '일일 한도'로
   바꿨다(`DartQuota.daily_limit()` 신설). 잔여는 오늘 진도를, 일일 한도는 완성 일수를 정한다.

---

## 5-b2. v1.2 — 사용자 FULL 크래시 수정 + TONE-MEASURE v1.0 구현

### 크래시 원인 (사용자 실환경 · Windows py3.14 · L1.PANEL1 즉사)
`MergeError: incompatible merge keys [0] dtype('O') and CategoricalDtype(...)`.
트레이스백 12줄 절단이 `merge_asof` 프레임을 숨겼다 — 실체는 **캐시 parquet 의
`krx_ohlcv_daily`(7,055,639행)가 `code` 를 category 로 저장**하고 있었고(과거 실행의 downcast
잔재), `merge_asof(by="code")` 는 키 dtype 이 정확히 같아야 한다. 이 컨테이너의 캐시에는
category 가 없어 여기선 통과하고 사용자 환경에서만 죽었다.
**수정**: 캐시가 파이프라인에 들어오는 유일한 관문(`QVFVault.get_table`)에서
`KEY_STR_COLS`(code·corp_code 등 12종)를 문자열로 강제(`_decat_keys`) + `build_cap_panel`
방어 + 전수조사 `§TM키` 조항이 **바로 그 입력(category 일봉+스냅샷)으로 실행 재현**한다.

### TONE-MEASURE v1.0 (`build/q27_tonemeasure.py` · QVF §6.2 ΔTONE_resid 정의 대체)
- **축 A**: 게이트 A1~A6(실측·증권사별 분해) · 리포트 유형 규칙분류(24개월 INITIATION 규칙) ·
  POS/NEG 분리(문장 마진투표 병렬계열) · **T2 broker 자기참조 차분**(400일 규칙·분기 단순평균·
  n_b) · 직교화 확장(report_type 비중·n_b·log ADTV). A1/A2 실패→축 A 꺼짐, A4 실패→T3 폴백,
  A6 미달→'검증 불가'(가중치 재조정 금지 — 그건 튜닝이다).
- **축 B**: B1 정규화 6단계+섹션분해(S1/S2/S3)+유사도 4종+셀 z(부호 −) · B2 DART 전용
  분류기(별도 학습·라벨 오염 명시)+자기이력 selfz(최소 4·`tm_selfz`)+펀더멘털 잔차
  ABTONE(부호 −) · B3 = 기존 하드팩트/이상현상. **문서 수집은 U-200 합집합 × 사업보고서만**
  (`fetch_dart_section_texts` — L1.FACTS 와 같은 targets).
- **결합**: `dAXISB` = 부호정렬 합성(성분 2개 이상일 때만; dNONFIN 단독이면 원값 후퇴 = 기존
  QVF 동작 비트 재현) · Score2 = 2·z(축B) + 1·z(ΔTONE_resid) − 배제(F2 형태가 실선정) ·
  INTERACT 는 보고전용 가설(F1 어블레이션에서만 페널티 검정 — §5.2 승격 대기).
- **어블레이션 11종 고정**(A1~A5·B1v·B2v·B3v·C1·F1·F2) + §6.1 필수비교 6종 + §6.2 BOTTOM
  비대칭 + 자체 BH-FDR 패밀리(§2.3). **폐기조건 TM[1~8]** 보고(부호 역전 시 뒤집지 않고
  해당 층 제외 — §7[7]).
- 합성 산식(dAXISB 동일가중)·INTERACT 페널티 1.0z 는 명세가 침묵하는 임의 선택 — 원장 기재.

전수조사 **32조항 · 272소검정**(TM 7조항 신규 — T2 차분 정답, B1 '숫자만 변경 → CHANGE<0.05
vs 내용 변경 → >0.30', selfz 확장윈도우 정답, 부호 정렬 상관, 게이트 판정 로직).
검증 4종(조립기·전수조사·SMOKE·원셀) 전부 통과. **B1/B2/게이트의 실데이터 검증은 미완** —
FULL 재실행에서 섹션 텍스트 수집(U-200 × 연 1회 ≈ 수천 호출)이 처음 돈다.

---

## 6. 다음 세션에서 할 일

### 1순위 — FULL 재실행 (명세 전수조사는 완료)

**두 세션이 독립적으로 전수조사를 했고 치명 결함 3건에서 결론이 일치했다** — Q축 3/5 사망,
`paired_diff_test` 언패킹, X0 키 오타. 서로 다른 합성 입력으로 같은 곳을 짚었다는 뜻이라
이 셋은 확정으로 본다. 수정도 동치였고(호출부 `valid=True` + 헬퍼 하드닝) 병합 시 둘 다 남겼다.

**그런데 상대 세션만 잡은 것이 4건 있다. 아래는 아직 열려 있다:**

| 조항 | 결함 | 실측 | 상태 |
|---|---|---|---|
| §3.4 | 정리매매가를 **손실일 때만** 인정 → 합병·공개매수 등 **상향 청산이 −100%** 로 계상 | 청산 종가 130(진입 100)인데 `fwd_ret = −1.00` | **미해결.** 실데이터로 건수를 세고 결정할 것 |
| §5.4 | Z_F 가 외국인·기관 z 를 **각각 표준화 후 평균** — '합산 순매수의 z' 가 아니다 | 결합 z 대비 상관 0.747 · 상위 30 자카드 0.429 | **명세 판단 필요** |
| §6.3 | 60~80 미달 시 경고가 없다(§7.4 는 경고한다) | 배제율 90% → 20종목인데 무경고 | 미해결 |
| §10.2 | 귀속 분해가 산문뿐, 표로 계산하지 않는다 | — | 미해결 |

> ⚠ **§3.4 를 고치려면 `L0.SPEC` 검정도 같이 고쳐야 한다.** 전수조사 §3.4 의 소검정
> "D 마지막 관측가가 '이익' → 규정대로 −100%" 는 **현재의 보수적 동작을 사전등록으로 못박은
> 것**이다. 상향 청산을 인정하도록 바꾸면 그 검정이 실패하는데, 그건 회귀가 아니라 의도된
> 변경이다. 검정을 먼저 고치고 코드를 고칠 것 — 반대로 하면 게이트가 막는다.
> (손실 조건이 있는 이유도 남아 있다: 소스가 폐지 직전에 종목을 드롭하면 마지막 정상가가
>  청산가로 둔갑해 '상장폐지 = 0% 손실'이 된다. 두 오류 방향의 교환관계이며 어느 쪽도 공짜가
>  아니다. 실데이터에서 각 경우가 몇 건인지 세는 것이 유일한 정직한 판단 근거다.)


§4-5 의 1·2번이 **선정과 판정을 바꾼다.** Q축은 2개 지표 → 5개 지표가 되어 VQ·VQF 의
U-200 이 달라지고, §10.4 ②는 '항상 발동' → '실제 검정'이 된다. **그 전에 산출된 성과 수치는
인용하지 말 것.** 재실행 후 확인할 것:

1. `Q축 완성 — … 지표 5개 중 평균 N개 가용` 의 N 이 2 가 아니라 4~5 인가
2. `②` 근거란이 `차이 HAC t ±X.XX · p 0.0XX` 로 찍히는가 (평균값 0.0xxx 가 아니라)
3. X0 스몰캡 벤치마크 표의 CAGR/Sharpe/MDD 가 nan 이 아닌가
4. pandas 버전 — `python -c "import pandas;print(pandas.__version__)"`. 3.x 면 §4-5 의 3·4가
   해당된다(이번 커밋으로 해결됨). 2.x 면 무해하다

### 검정 4종 (전부 통과해야 한다)

```bash
python3 tools/build_qvf.py        # 조립 + 문법·중복정의·정의전참조 검사
python3 tools/smoke_qvf.py SMOKE  # 파일 실행 (계약 → 전수조사 → 스모크 → 리허설)
python3 tools/audit_spec_qvf.py   # 전수조사만 따로 (조항 25 · 소검정 233)
# 원셀 실행 시뮬레이션 — __file__ 없이 exec. Colab 을 죽였던 버그의 유일한 재현 경로
```

전수조사는 이제 `smoke_qvf.py` 안에서도 `L0.SPEC` 스테이지로 돈다(약 1.8초).
개별 조항만 돌리려면 인자로 번호를 준다: `python3 tools/audit_spec_qvf.py 5 6`.

**새 검정을 쓸 때 지킬 것 두 가지:**
1. **'검정력 확인' 소검정을 같이 넣을 것.** 실패하는 입력을 만들 수 없는 검정은 통과해도
   아무것도 보장하지 않는다(계약 8개가 그랬다). 기존 검정들이 그 패턴을 따른다.
2. **전역을 만졌으면 `_SPEC_GUARDED` 에 이름을 넣을 것.** 안 넣으면 검증층이 본 실행의
   입력을 바꾼다. 복원 여부는 마지막에 자동으로 검정되지만, 목록에 없으면 검정도 안 된다.

모듈 로딩(`main()` 안 돌리고)은 러너의 `load_module()` 를 그대로 쓰면 된다:
```python
import sys; sys.path.insert(0, "tools")
from audit_spec_qvf import load_module
G = load_module()          # VAULT 스텁·로그 억제까지 끝난 전역 딕셔너리
G["axis_Q"](my_panel)
```

### 2순위 — 위 §5 의 미해결 항목 2개 (pykrx 실환경 확인 · KRX 로그인)

### 3순위 — PR #16 본문 갱신
성능·호출량 섹션이 한 세대 뒤처져 있다. FULL 실행 완주 후 실측으로 다시 쓸 것.

---

## 7. 사용자 응대 원칙 (이 세션에서 배운 것)

- **말이 아니라 숫자로 답한다.** "고쳤습니다"가 아니라 "상관 −0.008 → +0.986".
- **틀렸으면 내가 틀렸다고 먼저 말한다.** 이 세션의 결함 상당수가 내가 직전 커밋에서
  만든 것이었다(stdout 전역교체, DART 센티넬, PDF 범위 미축소).
- **"통과"를 근거로 삼지 않는다.** 계약 8개가 검정력 없이 통과하고 있었다.
- **한계를 숨기지 않는다.** 시총 폴백의 주식수 이월 근사, TONE 의 문서단위 대체 등은
  로그와 코드에 명시돼 있다. 지우지 말 것.
