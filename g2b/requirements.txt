# G2B-DEMAND-GRAPH-V1 실행 의존성
# 설치:  python -m pip install -r g2b/requirements.txt
pandas>=2.0
numpy>=1.24
pyarrow>=12.0        # parquet 저장/적재
scipy>=1.10          # lifecycle 연결요소(connected_components)
requests>=2.28       # 공공데이터포털 / OpenDART 호출
PyYAML>=6.0          # 사전등록 config
pytest>=7.0          # 회귀 테스트 (§92 §93)
